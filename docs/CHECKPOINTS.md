# ARGUS CHECKPOINTS

Stub reserved by the Phase 0 repository contract (MASTER_SPEC.md section 7).
This document is populated as the phase(s) that need it are implemented —
see MASTER_SPEC.md for the phase plan and docs/BUILD_STATE.md for current
progress. Writing detailed content here ahead of the corresponding phase
would risk drifting from what actually gets built, which MASTER_SPEC.md
section 101 (no backtest theater) and CORE-011 (truth outranks impressive
appearances) both argue against.
