# ARGUS Build State

Machine-and-human-readable state for session recovery (MASTER_SPEC.md section 8).
Every new implementation session must read this file before doing anything else.

```yaml
current_phase: 5  # Phase 5 (COPYABILITY + FORWARD INFORMATION VALUE) first submission complete, awaiting independent audit -- per argus-phase-5-001's explicit APPROVES_PHASE: 4 / AUTHORIZED_PHASE: 5
last_completed_phase: 5  # Phase 5 first submission complete (implements M1-M7, migration 0022, argus copyability report CLI, P5-11 replay-script output-dir refactor; sealed 14-row acceptance contract P5-01..P5-14, see orchestration/checkpoints/phase_5.md section D). Phase 5 itself is NOT yet orchestrator-approved -- this is the FIRST submission for Phase 5, pending independent audit.
last_orchestrator_approved_phase: 4  # argus-phase-5-001's own APPROVES_PHASE: 4 approved Phase 4 (PASS_WITH_DEFERRED_ENVIRONMENTAL_VALIDATION) in the SAME instruction that authorized Phase 5 -- this is the orchestrator's own explicit approval, not a self-approval by this session.
approved_commit: 354ed229eb4ba8c16622b008b7494b3687da525e  # argus-phase-5-001's own TARGET_COMMIT field value -- the commit the orchestrator's Phase 4 approval was made against. Phase 5 itself is not yet approved.
awaiting_orchestrator_review: true  # Phase 5 first submission complete this session -- checkpoint/bundle/handoff produced, awaiting independent audit per argus-phase-5-001

# PG17_COMPOSE_VALIDATION tracks whether `docker compose up postgres`
# (the actual postgres:17 image, per TECH-004 and compose.yaml) has been
# exercised end-to-end. It is DEFERRED, not PASS and not FAIL: functional
# correctness of the migration/application code was verified against a
# substitute PostgreSQL 16 server instead (see known_blockers below and
# docs/DECISION_LOG.md). Per explicit orchestrator instruction (2026-08-30):
# this deferral does NOT block starting Phase 1, but DOES block approving
# live readiness until it is closed out with a real postgres:17 run.
PG17_COMPOSE_VALIDATION: DEFERRED_ENVIRONMENTAL_CHECK

known_blockers:
  - "PG17_COMPOSE_VALIDATION = DEFERRED_ENVIRONMENTAL_CHECK. This
     implementation sandbox's egress policy blocks Docker Hub's image CDN
     (production.cloudfront.docker.com -> 403 at the proxy), so
     `docker compose up postgres` (the actual postgres:17 image) could not
     be pulled or exercised here. compose.yaml is unmodified and still
     targets postgres:17 as required by TECH-004 -- this is an environment
     limitation of this sandbox, not an architecture change. As a substitute,
     Phase 0 functional acceptance (migration-from-zero, DB roles,
     provider_usage grants, argus health, full pytest suite, missing-credential
     fail-closed behavior) was verified against the sandbox's
     locally-installed PostgreSQL 16 server, running the exact same Alembic
     migration and application code Compose would run against PostgreSQL 17.
     This PG16 run demonstrates the migration/application logic is correct;
     it does NOT demonstrate anything PostgreSQL-17-version-specific and
     must not be cited as PostgreSQL 17 validation. Closing this out requires
     running `make bootstrap && make up` (or equivalent) on a host with
     normal Docker Hub access and recording the result here and in
     docs/DECISION_LOG.md. See docs/DECISION_LOG.md for the full decision
     record. Per explicit orchestrator instruction, this deferred check does
     NOT block Phase 1, but IS required before live readiness can be
     approved."
  - "REALCHAIN_GOLDEN_FIXTURES = 9_OF_9_CATEGORIES_ONE_WITH_CAVEAT (round
     5, findings #1/#2/#3/#4 -- see below for how this improved on round
     4's honestly-reported 6/9). Round 2 imported 4 real fixtures from
     solana-labs/explorer (MIT); round 3 imported 6 more from
     0xjeffro/tx-parser (MPL-2.0); together these evidenced 6 of 9
     required categories: 'simple transfer' (both perspectives),
     SOL-to-token swap, token-to-SOL swap, token-to-USDC swap, multi-hop
     swap, and partial sell. Round 5 finding #4 fixed the parser's
     ambiguous-multi-asset handling, which as a side effect makes
     `real_mainnet_dca_close_dual_asset_transfer_in` (imported in round
     3, excluded from category count by round 4's correction below)
     resolve to `UNKNOWN`, ineligible -- satisfying category 7,
     'ambiguous multi-asset transaction', with no new fixture needed.
     Round 5 finding #3 searched three named candidate repositories and
     imported two more real fixtures: `real_mainnet_failed_nft_sale`
     (milktoastlab/SolanaNFTBot, MIT -- a genuine failed on-chain Magic
     Eden sale, `meta.err` set, extracted from its TypeScript-module
     wrapper via a new deterministic `extract_ts_const_export_default`
     transform step), satisfying category 9, 'failed on-chain
     transaction', cleanly; and
     `real_mainnet_orca_increase_liquidity_multi_asset_outflow`
     (quellen-sol/ingestooor, GPL-3.0 -- a genuine Orca Whirlpool
     increaseLiquidity call), mapped to category 8, 'multiple
     token-account/LP-style action', **with an explicit caveat**: the
     parser's own `LP_ACTION` label does not fire for this specific
     transaction (only one non-SOL asset is directly signer-owned; the
     LP position is held by a program-derived vault account instead), so
     it resolves via the ambiguous-multi-asset-outflow branch to
     `UNKNOWN` instead -- the substantive requirement (a real
     multi-token-account liquidity transaction, correctly never a
     confident single-asset trade) is satisfied, the specific label is
     not. **All 9 of 9 round-1-required categories now have real-chain
     evidence**, 8 without caveat and 1 (LP action) with the caveat above
     -- see tests/golden/fixtures/real/SEARCH_LOG.md's 'Round 5' section
     for the full search log, candidate-repository evaluation, and
     license-compatibility reasoning (including for GPL-3.0, reused here
     as one immutable verbatim data file, not linked code). Round 2/3/4's
     own checkpoints and phase-history rows below are left unmodified as
     immutable history of what was claimed at the time.
     ROUND 6 UPDATE: independent re-review (round 6 finding #3) found the
     round-5 LP/multiple-account fixture
     (real_mainnet_orca_increase_liquidity_multi_asset_outflow) had only
     one material non-SOL token account from the reviewed wallet's own
     perspective -- it did not actually satisfy the category. Per the
     round-6 instruction's explicit 'source a better fixture; do not
     relabel it', it was replaced (same upstream repo/commit, different
     wallet perspective:
     real_mainnet_orca_close_position_multi_account, wallet
     JC8m5y9D7atuzD7mToWN8VVrtxyxCXQ3SFWMHFiLZagN, the transaction's
     actual signer) with a fixture independently proven, via a new
     account-level delta oracle (compute_account_level_deltas /
     ExpectedAccountAssetDelta), to have two genuinely distinct material
     token accounts. All 9 of 9 required categories still have real-chain
     evidence, with NO remaining label caveat on this category (the
     parser correctly still emits UNKNOWN/ineligible for it, which the
     round-6 instruction explicitly accepts for this category as long as
     the underlying multi-account evidence is independently proven -- see
     orchestration/checkpoints/phase_1_remediation_6.md section E item 4
     and tests/golden/fixtures/real/SEARCH_LOG.md's 'Round 6' section).
     Round 6 also closed a gap where fixture provenance's saved
     `git ls-tree` line only proved self-consistency, not an actual
     offline-verifiable commit->tree->blob object chain (finding #2) --
     see golden_fixtures.py's GitTreeAttestation/verify_git_object_chain."
```

## Phase history

| Phase | Status | Commit | Notes |
|-------|--------|--------|-------|
| 0 | ORCHESTRATOR-APPROVED (`argus-phase-1-001`, 2026-08-31) | b838558f7eae1eac8d3559c7826ab340d604d916, remediated at ca74d09b3f976a5726fe46c1a8ea59d7bbdd3ad7 (history rewritten 2026-08-30 to scrub inert dev-only placeholder credential strings — see docs/DECISION_LOG.md; these are the post-rewrite hashes) | Foundation scaffold: repo layout, uv env, Compose+Postgres, Alembic baseline + DB roles, config/spec hashing, clock abstraction, structured logging, CLI skeleton, FastAPI skeleton, health framework, provider_usage schema, checkpoint bundle framework. Remediated per orchestrator feedback: removed all hardcoded fallback DB passwords (migrations/versions/0001_*.py, compose.yaml, src/argus/db/connection.py) in favor of required env vars that fail closed via MissingCredentialError; corrected checkpoint STATUS to not claim an unconditional PASS while PG17-via-Docker-Compose remains untested (see PG17_COMPOSE_VALIDATION above). 41/41 tests pass, 93% coverage, ruff+mypy clean. Approved by the ARGUS ORCHESTRATOR at commit `141af487fcfdff41d1597c19ea062139f5427f52` as `PASS_WITH_DEFERRED_ENVIRONMENTAL_VALIDATION`. See runtime/reports/checkpoint_phase_0.txt for the full checkpoint. |
| 1 | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-001`, target `141af487fcfdff41d1597c19ea062139f5427f52`) | 28a88f74d28e70542050f5d5e8d9a9d139f26bb8 | Live chain data acquisition + deterministic canonical parsing: Helius RPC/WSS adapter, DexScreener/GeckoTerminal/Jupiter adapters (no signing), fast-path+truth-path reconciliation with per-wallet watermarks and DEGRADED gating, durable clock-anomaly detection wired into reconciliation, immutable `chain_events`/`swaps`/`clock_health_events` ledger, generic balance-delta swap parser (11 golden fixtures), P0-P6 priority scheduler, provider usage accounting with 70/85/95% warnings wired into every real adapter call, HTTP retry/backoff, provider capability/history/usage probe CLI. 204 tests passing, 91% coverage, ruff+mypy clean. STATUS `PASS_WITH_DEFERRED_ENVIRONMENTAL_VALIDATION`: acceptance criteria 1-2 (live Solana RPC/WebSocket) NOT TESTED -- no `HELIUS_API_KEY` configured and no general internet egress in this sandbox; no end-to-end stream-manager orchestration loop exists yet (each piece is built and tested in isolation). See `orchestration/checkpoints/phase_1.md` for the full 27-item disposition and disclosed gaps. NOT yet orchestrator-approved -- only the orchestrator may advance `last_orchestrator_approved_phase`. **Superseded by the remediation row below** -- an independent orchestrator audit (`argus-phase-1-remediation-001`) rejected this self-assessment as overstated; this row is kept unmodified as immutable history. |
| 1 (remediation round 1) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-remediation-001`, target `32c2898ab8c278c2f75f4a2f40fedd9d35b24b08`) | 83bb38497ac3af1402f38dabee6858e00ce2e9fb | Remediated all 10 audit findings from `argus-phase-1-remediation-001`: production `IngestionManager`/`argus ingest run` composing the WebSocket stream, reconciliation, and clock monitor into real runtime behavior; bounded cursor-based truth-path pagination that never silently truncates a gap; an auditable append-only commitment-observation model (replacing the dead `confirmed_at`/`finalized_at` columns) with regression/conflict rejection; parsing wired end-to-end into `swaps` persistence via a real `SqlSwapRecorder`; typed provider-contract validation replacing bare `isinstance(dict)` checks; usage accounting that survives transport exhaustion and is wired into the manager's real streaming code path; a dispatch-count-bounded scheduler starvation guarantee; 6 real `tests/replay` tests (was 0 collected). 260 tests passing, 84% coverage, ruff+mypy clean, alembic downgrade-to-base/upgrade-to-head clean. STATUS `PARTIAL_NOT_TESTED_REALCHAIN_FIXTURES`: 25 of 26 mandatory acceptance criteria PASS; criterion 15 (authenticated real-chain golden fixtures) is honestly NOT TESTED/blocked -- this sandbox has no general internet egress and no already-available safe source of authentic transaction data, per the instruction's own explicit fallback for this exact case. See `orchestration/checkpoints/phase_1_remediation_1.md` for the full 26-item disposition. NOT yet orchestrator-approved -- only the orchestrator may advance `last_orchestrator_approved_phase`. **Superseded by the remediation round 2 row below** -- an independent orchestrator audit (`argus-phase-1-remediation-002`) rejected round 1 as still insufficient; this row is kept unmodified as immutable history. |
| 1 (remediation round 2) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-remediation-002`, target `04f367b8e03e99718812f872a34e73e170c44f0d`) | e44b5885b8aa02105e13051af4045e23e17b084c | Remediated all 12 audit findings from `argus-phase-1-remediation-002`: session-per-operation unit-of-work (no `AsyncSession` shared across concurrent tasks); explicit subscription-acknowledgement lifecycle with three independent recovery dimensions (stream/reconciliation/clock) so no single dimension can restore OK alone; structured task supervision (`IngestionManagerFailure`, fail-closed on any child-task death); finalization sweep wired into a real manager background loop; deterministic atomic commitment derivation (monotonic `sequence` column, `pg_advisory_xact_lock` serialization, full-same-level-state conflict validation, `commitment_observation_rejections` audit table); mechanical append-only enforcement at the database role layer; typed canonical provider response models (`argus.providers.models`) replacing provider-shaped dicts; usage records exactly one terminal outcome decided after decode/validation (including a visible signal on recorder failure); durable versioned `parse_attempts` ledger + `argus ingest reparse`; pagination continuity/ordering validation; a cancelled scheduler submission can never execute; and 4 genuine real-chain golden fixtures (of 9 required categories) sourced via this sandbox's working GitHub read access, imported through a new offline `argus fixtures import-real-chain`/`validate-real-chain` tool. 327 tests passing, 85% coverage, ruff+mypy clean, alembic downgrade-to-base/upgrade-to-head clean through migration 0005. STATUS `PARTIAL_REALCHAIN_FIXTURES_4_OF_9_CATEGORIES`: 26 of 27 mandatory acceptance criteria PASS; criterion 21 (real-chain golden fixtures) is honestly PARTIAL -- 4 of 9 required categories now genuinely real-chain evidenced, the remaining 8 (every DEX-swap-shaped category) are NOT TESTED since no repository searched embeds real swap-transaction bytes. See `orchestration/checkpoints/phase_1_remediation_2.md` for the full 27-item disposition and `tests/golden/fixtures/real/SEARCH_LOG.md` for the search log. NOT yet orchestrator-approved -- only the orchestrator may advance `last_orchestrator_approved_phase`. **Superseded by the remediation round 3 row below** -- an independent orchestrator audit (`argus-phase-1-remediation-003`) rejected round 2 as still insufficient; this row is kept unmodified as immutable history. |
| 1 (remediation round 3) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-remediation-003`, target `87a0e2efe329512a78f81331da24a85adf62bbbe`) | 81dd46cbfa3a46dd97c2f59a92ec62a42ab4fda9 | Remediated all 6 audit findings from `argus-phase-1-remediation-003`: evidence-bearing pagination boundary verification (a persisted boundary must be directly observed in the provider's own address-history sequence, never inferred from an empty/short page alone); every Helius RPC method's full nested contract validation now runs inside its single accounted usage operation (no malformed result can leave an "ok" usage row); streaming usage-recorder failures emit a visible `usage_recorder_failed` structured warning instead of disappearing via `contextlib.suppress`; `parse_attempts` durably records build/config/MASTER_SPEC/git identity (4 new NOT NULL CHECK-constrained columns, migration 0006, backed by a new `argus.config.git_commit_sha()` and `argus.parsing.generic_parser.PARSER_BUILD_HASH`); `sweep_finalization()` returns a typed `FinalizationSweepResult` distinguishing a genuine zero-promotion sweep from a provider/malformed-response/per-event-append failure, surfaced by the manager's own background loop; and 6 more genuine real-chain golden fixtures (of 9 required categories, now 7/9 total) sourced from `0xjeffro/tx-parser` (MPL-2.0). New `tests/integration/test_migrations.py` (6 tests) independently re-proves migration-from-zero/upgrade-from-0003/upgrade-from-0005/downgrade/idempotency/restart-safety against a disposable scratch database. 371 tests passing, 86% coverage, ruff+mypy clean, alembic downgrade-to-base/upgrade-to-head clean through migration 0006. STATUS `PARTIAL_REALCHAIN_FIXTURES_7_OF_9_CATEGORIES`: 17 of 18 mandatory acceptance criteria PASS; criterion 1 (real-chain golden fixtures) is honestly PARTIAL -- 7 of 9 required categories now genuinely real-chain evidenced, the remaining 2 ('multiple token-account/LP-style action', a genuinely failed transaction) are NOT TESTED since no repository searched (across either round) embeds either. See `orchestration/checkpoints/phase_1_remediation_3.md` for the full 18-item disposition and `tests/golden/fixtures/real/SEARCH_LOG.md` for the search log. NOT yet orchestrator-approved -- only the orchestrator may advance `last_orchestrator_approved_phase`. **Superseded by the remediation round 4 row below** -- an independent orchestrator audit (`argus-phase-1-remediation-004`) rejected round 3's 17/18 self-scoring as overstating fixture coverage and several runtime acceptance claims; this row is kept unmodified as immutable history. |
| 1 (remediation round 4) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-remediation-004`, target `a589e15c29937b140ae96bdfc2d75de62a9109c2`) | 9d51dcfbcf1c303da120d771cecda940ab51cf25 | Remediated all 8 audit findings from `argus-phase-1-remediation-004`: corrected real-chain fixture coverage from round 3's overstated 7/9 to the genuine 6/9 (`real_mainnet_ambiguous_multi_asset` renamed to `real_mainnet_dca_close_dual_asset_transfer_in` and excluded from the ambiguous category -- the parser resolves it decisively to TRANSFER_IN, not UNKNOWN); rebuilt real-chain fixture provenance to preserve exact upstream bytes (content-addressed `sources/` directory keyed by git blob SHA-1, an ordered hashed transform manifest, and independent offline rebuild-and-verify for all 10 fixtures); decoupled golden `expected_classification`/`expected_confidence` (now required, independently-asserted arguments) from the parser's own `observed_classification`/`observed_confidence`; deepened Helius contract validation (bool-as-int slot/blockTime/decimals rejection, `TokenAccountInfo` canonical model, non-object-transaction/missing-nested-field rejection) and fixed WebSocket ack matching (exact JSON-RPC id/version match, bounded connect/send/ack timeouts); made reparse selection and `swaps` versioning parser-artifact-aware (`parser_version` + `build_hash`, migration 0007, 6 new real-Postgres tests); removed the false historical-version `--parser-version` CLI flag in favor of an honest current-artifact-only design; made production git identity fail closed on a dirty/unverifiable checkout (`resolve_production_git_commit()`); and made a missing finalization source a typed `ok=False` misconfiguration instead of a false clean sweep. 420 tests passing, 86% coverage, ruff+mypy clean, alembic downgrade-to-base/upgrade-to-head clean through migration 0007. STATUS `PARTIAL_REALCHAIN_FIXTURES_6_OF_9_CATEGORIES`: 19 of 20 mandatory acceptance criteria PASS; criterion 5 (genuine ambiguous-transaction / failed-transaction fixtures) is honestly NOT TESTED/PARTIAL -- neither has been sourced in any round to date. See `orchestration/checkpoints/phase_1_remediation_4.md` for the full 20-item disposition and `tests/golden/fixtures/real/SEARCH_LOG.md` for the search log. NOT yet orchestrator-approved -- only the orchestrator may advance `last_orchestrator_approved_phase`. **Superseded by the remediation round 5 row below** -- an independent orchestrator audit (`argus-phase-1-remediation-005`) rejected round 4 outright (`FAIL_REMEDIATION_REQUIRED`) on 9 findings; this row is kept unmodified as immutable history. |
| 1 (remediation round 5) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-remediation-005`, target `2f436ae775c6185f820f59bc8dbef61ce0a95160`) | 6c7f4df1cce181dd54383b6dbb09f6be27df4471 | Remediated all 9 audit findings from `argus-phase-1-remediation-005` (which rejected round 4 outright, not merely PARTIAL, on 9 findings): replaced golden fixtures' flat `expected_classification`/`expected_confidence`/license strings with a typed, independently-reviewed `ExpectedOutcome` (wallet perspective, every asset delta, expected input/output, network fee, failed-tx status, confidence rule, reviewer method/rationale/evidence) and real `git ls-tree`-backed `GitTreeAttestation`/`LicenseEvidence` cryptographically binding upstream repo/commit/path/blob/license, folded into one `evidence_chain_hash` so any single-field edit is detectable offline; sourced real-chain fixtures for all 9 of 9 round-1-required categories (up from 6/9) -- a genuine failed on-chain transaction (`real_mainnet_failed_nft_sale`, via a new deterministic `extract_ts_const_export_default` transform step unlocking TypeScript-wrapped upstream sources) and a genuine multi-token-account Orca Whirlpool liquidity transaction (`real_mainnet_orca_increase_liquidity_multi_asset_outflow`, with an explicit documented caveat: it resolves via the ambiguous-multi-asset-outflow branch, not the `LP_ACTION` label), plus the previously-imported DCA-close fixture now genuinely satisfying the ambiguous-multi-asset category as a side effect of the parser fix below; made the generic parser fail-closed for ambiguous/NFT/LP/multi-hop assets (`SWAP_COMPLEX` no longer copy-eligible, decimals-zero legs never eligible, 2+ same-direction assets with no offsetting leg is `UNKNOWN` not a confident guess, new public `compute_asset_deltas()`); deepened Helius HTTP contract validation (strict-nonnegative-int slot/balance/fee fields, full accountKeys/preTokenBalances/postTokenBalances validation, `get_token_accounts` ownership cross-check and bounded decimals, immutable `TokenAccountInfo.raw`); fixed a WebSocket ack type-equality bug (`"id": true` could match request id 1 under Python's `==`), added early-notification buffering (a message arriving before the ack is preserved and replayed, never discarded), added a transport-level ping/pong liveness probe so a quiet-but-healthy connection is no longer reconnected on every receive-timeout, and bounded the close/cleanup path; fixed production git-identity override precedence (dirty/HEAD is now checked before an override is ever trusted, closing a spoofing path); and made migration 0007's downgrade fail closed with a precise reason when incompatible multi-build `swaps` data exists, rather than an opaque Postgres constraint violation, proven against a genuinely populated scratch database. 490 tests passing (up from 420), 87% coverage, ruff+mypy clean, alembic downgrade-to-base/upgrade-to-head clean through migration 0007 including new populated-data downgrade tests. STATUS `REALCHAIN_FIXTURES_9_OF_9_CATEGORIES_ONE_WITH_CAVEAT`: see `orchestration/checkpoints/phase_1_remediation_5.md` for the full acceptance-matrix disposition and `tests/golden/fixtures/real/SEARCH_LOG.md` for the complete search log. NOT yet orchestrator-approved -- only the orchestrator may advance `last_orchestrator_approved_phase`. **Superseded by the remediation round 6 row below** -- an independent orchestrator audit (`argus-phase-1-remediation-006`) rejected round 5 as `FAIL_REMEDIATION_REQUIRED` on 6 findings; this row is kept unmodified as immutable history. |
| 1 (remediation round 6) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-remediation-006`, target `fbe46c44861e489f65d55abac01eedc4934318a7`) | 6e4aa5a9a0e2cdb2f75f1465d3939e8d73002ba0 | Remediated all 6 audit findings from `argus-phase-1-remediation-006` (which rejected round 5 as `FAIL_REMEDIATION_REQUIRED`): production Git identity now distinguishes absent-Git from present-but-unverifiable-Git via an explicit `_GitCheckoutState` enum, closing a fail-open path where any `git status`/`rev-parse` failure on a genuinely present checkout was misread as "no checkout" and could accept an arbitrary override; fixture provenance now preserves a genuine offline-verifiable Git object chain (`GitTreeAttestation`/`attest_git_tree()`/`verify_git_object_chain()`, raw commit/tree object bytes, independently recomputed via git's own content-addressing) replacing round 5's self-referential saved `ls-tree` line; the golden oracle now preserves account-level deltas before by-mint aggregation (`compute_account_level_deltas()`/`ExpectedAccountAssetDelta`) and binds/checks record identity fields (category, chain, signature, slot, transaction_version, upstream_path) against the rebuilt payload rather than trusting them as inputs; the round-5 LP/multiple-account fixture was replaced (not relabeled) with `real_mainnet_orca_close_position_multi_account`, independently proven via account-level evidence to have two genuinely distinct material token accounts (see the ROUND 6 UPDATE note above); Helius HTTP validation deepened further (JSON-RPC envelope validation, `get_transaction` signature-identity binding, strict u64 numeric domains, ASCII-bounded raw-amount-string validation, `get_signature_statuses` required-key checks, deep alias-safe `_deep_freeze()` immutability replacing a shallow `MappingProxyType`); and the unattended watcher's pre-launch remote-freshness race is closed with a fresh final barrier (re-fetch + re-verify clean worktree, HEAD==remote, instruction-file hash-vs-committed-blob, instruction fields, target-commit, and phase authorization immediately before launch), reverting to `IDLE` (never consuming the instruction as `FAILED`) on any barrier failure. 547 tests passing (up from 490), 86% coverage, ruff+mypy clean, alembic downgrade-to-base/upgrade-to-head clean through migration 0007. STATUS: 19 of 20 mandatory acceptance-matrix items unconditional PASS, item 17 the standing permitted `DEFERRED_ENVIRONMENTAL_CHECK` for live Helius/PG17 connectivity -- see `orchestration/checkpoints/phase_1_remediation_6.md` section E for the full disposition. This round's own evidence cross-check (finding #6) also surfaced and honestly disclosed a pre-existing, previously-unreported commit-trailer-formatting defect (a trailing `Co-Authored-By`/`Claude-Session` paragraph breaks `git interpret-trailers`' recognition of an earlier `ARGUS-INSTRUCTION-ID:` paragraph as a real trailer on 3 of this round's own commits and 2 historical commits dating to rounds 1 and 5) -- see checkpoint section H; not corrected via history rewrite, reported for orchestrator disposition instead. **Phase 1 is now ORCHESTRATOR APPROVED** (`PASS_WITH_DEFERRED_ENVIRONMENTAL_VALIDATION`) at this exact commit, per instruction `argus-phase-1-5-001` (`APPROVES_PHASE: 1`, independently audited, not the implementation agent's own PASS claim) -- see the row below for what that instruction authorized next; `docs/DECISION_LOG.md` records the approval decision. |
| 1.5 | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-5-001`, target `2fbc566af74832bc6523648f60ba8cb60d98eb31`, `APPROVES_PHASE: 1`) | f334f70908e9744940571f7caffd29c515eb0dac | Phase 1.5 historical-data feasibility spike: established both required inputs automatically from already-reachable free/public sources (no `BOOTSTRAP_TOKEN_INPUT_REQUIRED`) -- a real pump.fun token (`5dNYcCZXEGfGgbdUdq7MMR7KLsNJLLLgL83wLH8Fpump`, from its own creation transaction) and a real candidate wallet (`JAMESC37CTVoFEt7TAEcqBjdjAfAWZiPR1YdWotAFjeQ`, 14 real transactions spanning ~1 year across 4 protocols), both sourced from GitHub repositories already used and license-vetted by this project (`0xjeffro/tx-parser`, `quellen-sol/ingestooor`) since this session's proxy rejects GitHub's open-ended search API and general Solana RPC/BigQuery-credentialed egress is blocked/unavailable. Test A (early-buyer reconstruction) recovered exactly 1 real buyer (the token creator's own bundled dev-buy) -- honest, non-fabricated, but severely incomplete since no further buyer discovery is possible without a live RPC or indexed-dataset credential this sandbox lacks. Test B (wallet history) reconstructed the 14 real transactions via the unmodified existing Phase 1 parser, finding a genuine, disclosed 43% `UNKNOWN` classification rate on real lending/yield-position activity (a parser-completeness gap, not a data gap). Test C cross-validated 28 real transactions (the token tx + 14 candidate-wallet + 13 supplementary-wallet transactions from the same source) via an independent, from-scratch raw-evidence recomputation against `compute_account_level_deltas()`'s actual output: 28/28 agreements, 0 disagreements. Test D measured this spike's own offline cost (0 RPC calls, 0 credits, ~1MB evidence, sub-10ms processing) and gave an explicitly-labeled, theoretical linear-extrapolation scaling estimate (~501 RPC calls/wallet assumption -> ~50,100 for 100 wallets, ~501,000 for 1,000), declining to fabricate a dollar/credit cost with no documented per-call price table in this repository. `HISTORICAL_DATA_PATH = PASS_WITH_LIMITATIONS`, with both limitations (unproven acquisition breadth; the 43% classification gap) carried forward explicitly, not smoothed over; `FAIL` was considered and rejected since a genuine (if minimal) result was recovered and the downstream interpretation architecture is positively proven correct. 551 tests passing (up from 547), ruff+mypy clean. No schema migration, no Phase 2 work, no credential entered/disclosed. See `orchestration/checkpoints/phase_1_5.md` for the full 14-item disposition and `orchestration/phase_1_5/evidence/PROVENANCE.md` for complete evidence citations. `last_orchestrator_approved_phase` remains `1` (only the orchestrator may advance it to `1.5`) -- this instruction's own explicit requirement. **Superseded by the remediation round 1 row below** -- an independent orchestrator audit (`argus-phase-1-5-remediation-001`) rejected this submission as `FAIL_REMEDIATION_REQUIRED` on one SPEC_BLOCKING/SAFETY_OR_INTEGRITY_BLOCKING finding; this row is kept unmodified as immutable history. |
| 1.5 (remediation round 1) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-5-remediation-001`, target `b68e37393370c7f9f3eb8860fecdaaa3f9c28696`, `APPROVES_PHASE: NONE`) | 3aa61b4d220c3211e4dca1ca46b18b1ab510376e | Remediated the one SPEC_BLOCKING/SAFETY_OR_INTEGRITY_BLOCKING finding from `argus-phase-1-5-remediation-001`: two authentic non-trade transactions (a real Solend `Withdraw Obligation Collateral and Redeem Reserve Collateral`, a real xStep `Stake`) were reported `SWAP_SIMPLE`/`is_copy_eligible=true` solely because each has a clean one-negative/one-positive balance shape -- confirmed via `git stash` that the pre-fix parser genuinely returned `is_copy_eligible=True` for the real Solend transaction. Added a deterministic **positive semantic proof gate** to `src/argus/parsing/generic_parser.py`: a new, centrally versioned `_SUPPORTED_SWAP_PROGRAM_IDS` registry of 4 program IDs (Jupiter Aggregator V6, Raydium Liquidity Pool V4, Orca Whirlpool, pump.fun), each independently cross-checked against this project's own already-hand-reviewed permanent golden-fixture evidence before being added; `ParsedTransaction.is_copy_eligible` now additionally requires positive instruction-level evidence (`matched_swap_program_id is not None`) that the transaction's own top-level or inner instructions actually invoked a registered trade venue -- a narrow allowlist/proof gate, not a Solend/xStep denylist, so any other unsupported program (present or future) correctly stays research-only rather than requiring a new denylist entry. `PARSER_VERSION` bumped to `generic_balance_delta_v2` (observable eligibility output changed for real evidence). Both named false positives are now ineligible; all 4 permanent golden real-chain fixtures already marked eligible before this round remain eligible (independently re-verified against the new gate, not merely re-asserted). Reran the Phase 1.5 analysis under the corrected parser: delta-arithmetic agreement (28/28) is now reported separately from semantic eligibility validation (4/28 copy-eligible, each with its cited program) so the two claims are never conflated; `HISTORICAL_DATA_PATH = PASS_WITH_LIMITATIONS` unchanged, now resting on a corrected foundation. 8 new golden-fixture tests plus 2 new Phase 1.5 tests (10 new; 563 total, up from 553 before the fix's own delta -- see checkpoint for the exact breakdown), ruff+mypy clean, no schema change, no credential entered/disclosed. See `orchestration/checkpoints/phase_1_5_remediation_1.md` for the full 14-item disposition. `last_orchestrator_approved_phase` remains `1` -- this remediation approves no phase. **Superseded by the remediation round 2 row below** -- an independent orchestrator audit (`argus-phase-1-5-remediation-002`) rejected round 1's gate as still insufficient (program identity mistaken for swap-instruction identity); this row is kept unmodified as immutable history. |
| 1.5 (remediation round 2) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-1-5-remediation-002`, target `5d85848ab5bff397a192a0868ffcf1077b691706`, `APPROVES_PHASE: NONE`) | f4ed7893849128257b3b5e62f44b93b779ee50c8 | Remediated the one SPEC_BLOCKING/SAFETY_OR_INTEGRITY_BLOCKING finding from `argus-phase-1-5-remediation-002` (`P15-R2-001`): round 1's gate proved only that *some* instruction invoked an allowlisted program, never that the matched instruction was itself a swap -- reproduced directly against `TARGET_COMMIT`'s real code (all 6 representative adversarial probes genuinely returned `is_copy_eligible=True` pre-fix, including the instruction's own Orca/Raydium/pump.fun + non-swap-log audit probe). Replaced `_SUPPORTED_SWAP_PROGRAM_IDS` with a **program-AND-instruction-discriminator registry** (`_SWAP_INSTRUCTION_REGISTRY`) binding the resolved program ID, the SAME instruction's own decoded `data`, and an exact registered discriminator to one canonical instruction object; added a strict, local, bounded base58 decoder (`_decode_base58_strict`, fixed alphabet, canonical round-trip validation, no new dependency); extended `ParsedTransaction` with `matched_semantic_label`/`matched_discriminator_hex` (all three fields now required for `is_copy_eligible`). All 4 registry pairs (Jupiter V6 `shared_accounts_route`, Raydium LP V4 `swap_base_in`, Orca Whirlpool `swap`, pump.fun `buy`) independently derived by decoding each cited authentic fixture's own raw instruction data -- never from memory or documentation; the real Orca `DecreaseLiquidity`/`CollectFees`/`ClosePosition` non-swap discriminators (from `real_mainnet_orca_close_position_multi_account.json`) are proven absent from the registry. Honest disclosure: `suppl_13_titan_swap_with_fees_2.json`, eligible under round 1's program-only check, correctly becomes ineligible under round 2 -- its actual Raydium invocation's discriminator (`0x10`) is not the registered `swap_base_in` (`0x09`); the instruction explicitly permits this ("failing closed is required"). Added all T1-T11 required tests (T1 missing data, T2 non-swap discriminator including the authentic Orca bytes verbatim, T3 program/discriminator mismatch, T4 log-text-cannot-grant-eligibility, T5 Solend/xStep extended to assert no semantic match at all, T6 fixed independent oracle against every registry pair, T7 altered-evidence-fails-closed, T8 malformed-base58, T9 regression sweep, T10 deterministic reparse, T11 a hand-written Phase 1.5 oracle that does not import the production registry). `PARSER_VERSION` bumped to `generic_balance_delta_v3`. 613 tests passing (up from 563: golden 95 (was 46), phase_1_5 7 (was 6)), ruff+mypy clean, no schema change, no credential entered/disclosed. See `orchestration/checkpoints/phase_1_5_remediation_2.md` for the full 14-item disposition. `last_orchestrator_approved_phase` remains `1` -- this remediation approves no phase. |
| 2 | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-2-001`, target `c3148cc191de58ecab9b11cd05291cc8ffe45455`, `APPROVES_PHASE: 1.5`) | bd35f3a7a95d6c6b977be9e421c5ea16779e472c | Phase 2 (TOKEN + WALLET DISCOVERY): 11 new tables (migration 0008) -- tokens, token_mint_validations, reference_asset_prices, token_market_snapshots, token_winner_milestones, archaeology_triggers, archaeology_runs, wallets, wallet_discovery_events, early_buyers, token_negative_controls -- with least-privilege role grants and idempotency/partial-unique constraints throughout. Deterministic on-chain mint validation (two evidence paths: live `getAccountInfo`, and this sandbox's actual free-first `committed_transaction_token_balance_evidence` path) never treats address shape as validation. Bootstrap-token importer, market-snapshot/reference-price point-in-time ledger (NULL over false precision), versioned winner-milestone baseline detection (`MAJOR_WINNER>=10x`/`MONSTER>=20x`/`EXTREME>=50x`, zero-liquidity launch instant correctly excluded from baseline), automatic archaeology-trigger creation/consumption, deterministic order-independent early-buyer extraction ("tag, don't delete"), permanent wallet-discovery provenance (`exclusion_reason` DB-CHECK-constrained to `DISCOVERY_CONTAMINATION` on every row), and negative-control schema round-trip -- all wired through real `argus tokens import-bootstrap`/`argus discover archaeology-run`/`argus discover watch-replay` CLI commands, not test-only helpers. Real historical-token demonstration reused the Phase 1.5-verified pump.fun token (`5dNYcCZXEGfGgbdUdq7MMR7KLsNJLLLgL83wLH8Fpump`): mint `VALID` from real evidence, 2 early buyers recovered from the real creation transaction (with an honest disclosed limitation -- one is very likely the bonding-curve program's own reserve account, not a human trader, since raw balance-delta extraction cannot itself distinguish the two), and a `MAJOR_WINNER` (12x) milestone demonstrated end-to-end from explicitly-labeled REPLAY synthetic market data (never claimed as real price history) -- full report in `orchestration/phase_2/DEMONSTRATION.md`. All 11 required P2-T1..T11 acceptance tests implemented and passing (40 focused Phase 2 tests). Fixed a genuine pre-existing defect this phase's own new cross-table foreign key surfaced: `src/argus/domain/__init__.py` was empty, so a process importing only some domain submodules could hit `NoReferencedTableError`; fixed via eager submodule imports. 653 tests passing (up from 613), ruff+mypy clean, migration-from-zero/upgrade-from-0007 clean through 0008, 12/12 real-chain fixtures ok, secret scan clean, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_2.md` for the full 14-item disposition. This instruction's own explicit Phase 1.5 approval is applied above (`last_orchestrator_approved_phase: 1.5`); **Phase 2 itself remains NOT orchestrator-approved** -- this instruction's own explicit "Do not mark Phase 2 approved." **Superseded by the remediation round 1 row below** -- an independent orchestrator audit (`argus-phase-2-remediation-001`) rejected this submission as not approved on 8 frozen findings; this row is kept unmodified as immutable history. |
| 2 (remediation round 1) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-2-remediation-001`, target `6bde9fdf6d56c38517854700e8863d9103e831aa`, `APPROVES_PHASE: NONE`) | 16737ca851ec51a528f4251fa94be3ef8ae84fc9 | Remediated all 8 frozen findings (P2-R1 through P2-R8) from `argus-phase-2-remediation-001`: corrected mint-account discrimination (`_classify_mint_account_layout()` replaces a bare `len(decoded) < 82` check that accepted a 165-byte legacy SPL token *account* as a valid Mint; a real SPL Token-2022 `AccountType` discriminator byte now genuinely distinguishes Mint from Account for extended accounts) and made mint-validation evidence handling fail closed on conflicting matching token-balance entries while persisting available `chain_time` (P2-R1+P2-R8); added a real, typed, provider-neutral historical acquisition service (`src/argus/tokens/historical_acquisition.py`, new file) over the existing Phase 1 `ChainProvider` contract, wired through a new production CLI command `argus discover acquire-and-run-archaeology`, explicitly handling multiple pages, duplicate item/page (both immediately-repeated-cursor and multi-step-cycle forms), premature short pages, safety-ceiling exhaustion, timeout/rate-limit/malformed-response provider failures, and per-transaction fetch failures -- proven by a new 14-test deterministic-fake-provider contract suite, `tests/unit/test_historical_acquisition.py` (P2-R2); made early-buyer ordering genuinely deterministic across `PYTHONHASHSEED` values (a full `(slot, signature, wallet_address)` sort key replacing an implicit Python `set`/`dict` iteration-order tie-break, proven via a real subprocess test) and added an evidence-grounded `ownership_classification` (transaction-signer-set membership) so a known pump.fun bonding-curve reserve PDA is no longer promoted into `wallets`/`early_buyers`/discovery events while its raw evidence is still preserved (P2-R3); made winner-milestone evaluation confidence-aware (`_is_reliably_tradable()` gates baseline/peak selection to HIGH/MEDIUM confidence only, closing the exact "12x LOW-confidence probe produced MAJOR_WINNER" scenario the finding named) (P2-R4); added a bounded, restart-safe automatic trigger consumer/executor (`run_next_pending_trigger`/`run_all_pending_triggers_for_token`, wired through a new CLI command `argus discover run-pending-trigger` with no `--trigger-id` option at all) so a milestone-triggered archaeology run executes without a human copying a trigger ID between commands (P2-R5); refactored `run_archaeology` into three independently-committing transaction phases (claim / extract+persist outputs / terminalize) plus a restart-recovery reaper (`reap_stale_archaeology_runs`), proven via a 4-point real-Postgres crash-injection matrix so a process crash at any required boundary leaves recoverable, non-lost attempt provenance (P2-R6); widened `token_market_snapshots.supply_raw`/`early_buyers.amount_raw` from signed `BigInteger` to a new `U64Numeric` type (`NUMERIC(39,10)` storage with range/integrality CHECK constraints, chosen after direct empirical proof that `NUMERIC(p,0)` silently rounds rather than rejects a fractional value) via new migration `0009_phase2_u64_raw_quantities.py`, with a fail-closed downgrade path and boundary round-trip tests for 0/1/2^63/2^64-1 (P2-R7). All 8 frozen remediation acceptance-test items (R1-R7 plus the regression item) pass. 702 tests passing (up from 653), 85% coverage, ruff+mypy+format clean, 12/12 real-chain fixtures ok, secret scan clean, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. A fresh, from-clean-database, real end-to-end CLI re-run of the demonstration against the real pump.fun evidence confirms the corrected behavior directly via Postgres queries -- `orchestration/phase_2/DEMONSTRATION_REMEDIATION.md` (new file; the original `orchestration/phase_2/DEMONSTRATION.md` is left unmodified as immutable historical record). See `orchestration/checkpoints/phase_2_remediation.md` for the full 8-finding/8-acceptance-test disposition. `last_orchestrator_approved_phase` remains `1.5` -- this remediation approves no phase. **Phase 2 remains NOT orchestrator-approved.** **Superseded by the remediation round 2 row below** -- an independent re-audit (`argus-phase-2-remediation-002`) found 7 of the 8 findings closed but left one narrow P2-R2 acceptance case open; this row is kept unmodified as immutable history. |
| 2 (remediation round 2) | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-2-remediation-002`, target `c99341a9c767c006cfe96fa4948dd54a9efe712b`, `APPROVES_PHASE: NONE`) | da7d09ec8d78f38906e69e7353db39ea8d18e8e7 | Fixed the one narrow remaining P2-R2 acceptance case left open by the round-2 re-audit: `acquire_historical_transactions()` (`src/argus/tokens/historical_acquisition.py`) gained an optional `expected_oldest_slot` boundary parameter -- when a caller supplies an independently known expected historical boundary (e.g. a token's own known creation slot), an empty/short pagination page is trusted as genuinely `COMPLETE` only once the walk has actually observed a signature at or before that slot; a premature truncation before then reports `PARTIAL` with the unsatisfied boundary explicitly named in `known_gaps`, preserving every signature/transaction already acquired. Omitting the parameter preserves the exact prior (round-1) unconditional short/empty-page-is-complete behavior, directly proven via a dedicated regression test using the identical page shape that reports `PARTIAL` with the boundary supplied. Wired through a new `--expected-oldest-slot` option on `argus discover acquire-and-run-archaeology` (real production CLI surface, not a test-only argument). 4 new frozen remediation-002 acceptance tests (module `test_historical_acquisition.py` 14 -> 18 tests). Exactly 3 files touched (`src/argus/cli.py`, `src/argus/tokens/historical_acquisition.py`, `tests/unit/test_historical_acquisition.py`) -- none of the seven closed P2-R findings' own code was revisited, only their regression tests re-run (all pass unchanged). 706 tests passing (up from 702), 85% coverage (unchanged), ruff+mypy+format clean, 12/12 real-chain fixtures ok, secret scan clean on the changed files. See `orchestration/checkpoints/phase_2_remediation_2.md` for the full defect-to-code/test mapping and the 4-item frozen acceptance-test disposition. `last_orchestrator_approved_phase` remains `1.5` -- this remediation approves no phase. **Phase 2 remains NOT orchestrator-approved.** |
| 3 | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-3-001`, target `c4adf963ed3a0cae815867b6cc97b6aa5b47f48a`, `APPROVES_PHASE: 2`) | f2e69423c1f93beb657ccc0bc415828ac2de046b | Phase 3 (WALLET RECONSTRUCTION + UNBIASED QUALIFICATION): 6 new tables plus `wallets.current_tier` (migration 0010) -- `wallet_history_quality`, `wallet_positions`, `wallet_metrics_snapshots`, `wallet_score_snapshots` (the phase's audit-critical decision ledger, `FullIdentityMixin`), `wallet_tier_history`, `wallet_cluster_links` -- with least-privilege role grants (including `wallets`' first ever `UPDATE` grant, for `current_tier`). Deterministic Decimal-exact V1 weighted-average-cost position reconstruction from existing `swaps` evidence (never a new raw-event table); position/history confidence (`HIGH/MEDIUM/LOW/UNRESOLVED` and `HIGH/MEDIUM/LOW/UNKNOWN`); a **structural** discovery-contamination firewall (`score_wallet()` computes `qualification_score` from a separately-filtered position set that never contains a contaminated token, and `descriptive_score` from an independent full pass -- proven byte-identical/measurably-different at both the pure-function and real-Postgres-service level, the phase-blocking MASTER_SPEC critical test); the frozen V1 qualification-score weights (never retuned) with a missing-component neutral-prior-imputation rule that lowers confidence rather than fabricating or redistributing weight; the frozen V1 sample-size gate (>=20 closed positions, >=10 distinct tokens, completeness not LOW/UNKNOWN) with deterministic proportional shrinkage (including a completeness-only shrinkage term, a genuine defect found and fixed during this run's own required-test-writing -- see checkpoint section K) rather than a fixed score cap; lottery-dominance flagging (>70% largest-position-share penalty, never automatic rejection); point-in-time recency decay; initial pairwise wallet-cluster-link evidence consumption (never real-world identity claims); and the full 9-state wallet tier lifecycle with immutable, timestamped transitions (proven via a real Postgres cluster-link-driven QUARANTINE transition that does not rewrite the wallet's earlier DISCOVERED transition row) -- all wired through a real `argus wallets reconstruct-and-score` CLI command, not a test-only helper. All 9 required test categories implemented and passing (12 unit + 3 integration tests against real Postgres). Required five-wallet sample report honestly reports `PHASE_3_CANDIDATE_SAMPLE_BLOCKED`: exactly 1 genuine candidate wallet exists in this sandbox from already-authorized authentic evidence (this project's one real pump.fun creation-transaction evidence source, unchanged since Phase 1.5/2), with zero real `swaps` evidence and therefore `history_completeness=UNKNOWN`/`qualification_score=descriptive_score=50.00` (the honest neutral prior) -- the frozen thresholds were not loosened and no wallet history was fabricated to reach 5; see `orchestration/phase_3/SAMPLE_REPORT.md`. 721 tests passing (up from 706), ruff+mypy+format clean, migration-from-zero/upgrade-from-0009/downgrade-then-reupgrade clean through 0010, 12/12 real-chain fixtures ok, secret scan clean, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_3.md` for the full 13-item build-surface and 8-item frozen-acceptance disposition. This instruction's own explicit Phase 2 approval is applied above (`last_orchestrator_approved_phase: 2`); **Phase 3 itself remains NOT orchestrator-approved** -- this instruction's own explicit "must NOT become 3 until a later orchestrator approval." **Superseded by the remediation row below** -- an independent audit (`argus-phase-3-audit-001`) found this submission `FAIL_REMEDIATION_REQUIRED` on 7 frozen findings; this row is kept unmodified as immutable history. |
| 3 (remediation) | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-3-remediation-001`, target `69a8de622b1977f92999ca680fcb8d851ba78c9f`, `APPROVES_PHASE: NONE`) | 5713e9bd86011ae1033507fbdab349cc3dc5fdbd | Remediated all 7 frozen findings (P3-R1 through P3-R7) from independent audit `argus-phase-3-audit-001`: enforced one knowledge-time cutoff across the complete scoring path (P3-R1 -- every evidence query in `reconstruct_and_score_wallet` now bounded to `<= now`: `Swap.first_seen_at`, `WalletDiscoveryEvent.created_at`, `EarlyBuyer.created_at`, `WalletClusterLink.as_of AND created_at`; `reconstruct_positions_for_wallet` independently excludes a malformed/future-dated `block_time` rather than clamping it to full recency); made history completeness evidence-bound and token-account-aware (P3-R2 -- a new structured `AcquisitionManifest`/`TokenAccountCoverage` replaces the free-text `--acquisition-status` CLI flag; HIGH now requires both a complete wallet-address walk and complete/genuinely-enumerated-empty associated-token-account coverage, never a caller-typed claim); implemented round-trip-safe, quote-safe weighted-average inventory (P3-R3 -- `position_reconstruction.py` rewritten with a `has_activity`-gated state machine emitting one `ReconstructedPosition` per independently-closed round trip, never one merged lifetime row; an incompatible-quote-asset leg is excluded from quantity/cost math, preserved as a raw reference, forces LOW confidence); materialized all five metric windows (P3-R4 -- `qualification_service.py` now writes LIFETIME/180D/90D/30D/7D `WalletMetricsSnapshot` rows every run from the same bounded, contamination-filtered manifest, an empty window explicit zero/null rather than a LIFETIME copy); corrected lottery/drawdown/usable-outcome metrics (P3-R5 -- `largest_trade_contribution_pct` now divides by estimated net lifetime P&L rather than gross positive gains, `None` when net P&L <= 0; drawdown equity curve ordered by `final_exit_at`; `distinct_tokens` counts only tokens with a closed usable outcome); made one canonical adjusted score drive persistence and tiers (P3-R6 -- the cluster-uncertainty penalty is now folded via `dataclasses.replace` into ONE final `ScoringResult` before both persistence and tier evaluation, proven byte-identical at the real-service level via a cluster-penalty-crossing-an-A/B-cutoff integration test; `tier_lifecycle.py`'s `current_tier is None` forced-DISCOVERED special case removed, closing an exact-replay-non-idempotency bug; `missing_required` now includes `forward_information`; `_score_equal`/history-row equality expanded to full semantic decision identity; `BUILD_HASH` now covers all 6 Phase 3 artifact files); and repaired the checkpoint protocol in fresh evidence (P3-R7 -- new `orchestration/checkpoints/phase_3_remediation.md`/`orchestration/bundles/phase_3_remediation.txt` correctly terminated; 2 new regression tests proving the pre-existing, UNCHANGED `validate_checkpoint_content` validator already correctly rejects the real historical malformed `phase_3.md`; historical `phase_3.md`/`phase_3.txt` preserved byte-for-byte unmodified as evidence). New migration `0011_phase3_remediation_point_in_time_and_ledger_integrity.py` adds `round_trip_index`/`input_manifest_digest` (positions), `input_manifest_digest` (score snapshots), `acquisition_manifest` (history quality), and widens the two trade-contribution columns from `Numeric(6,5)` to `Numeric(20,6)` (the corrected net-PnL-denominator ratio can legitimately exceed 1.0); its `upgrade()` deliberately clears the 4 derived Phase 3 decision tables (never raw evidence) since their pre-remediation rows reflect buggy computation this remediation replaces -- disclosed explicitly, not silent. All 9 required prospective acceptance-test categories pass (11 new unit tests, 4 new integration tests against real Postgres). 738 tests passing (up from 721), ruff+mypy+format clean, migration-from-zero/upgrade-from-0010/downgrade-then-reupgrade clean through 0011, 12/12 real-chain fixtures ok, secret scan clean, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. The accepted `PHASE_3_CANDIDATE_SAMPLE_BLOCKED` result (`orchestration/phase_3/SAMPLE_REPORT.md`) is unchanged, per this instruction's own explicit non-blocking disposition of that item. See `orchestration/checkpoints/phase_3_remediation.md` for the full 7-finding/9-test-category disposition. `last_orchestrator_approved_phase` remains `2` -- this remediation approves no phase. **Phase 3 remains NOT orchestrator-approved.** **Superseded by the remediation round 2 row below** -- an independent re-audit (`argus-phase-3-remediation-audit-001`) found round 1's remediation only PARTIALLY sufficient, with one new critical finding (a data-loss regression); this row is kept unmodified as immutable history. |
| 3 (remediation round 2) | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-3-remediation-002`, target `3fb7d5675bf4b6c1c497dad08eb319a0e349d188`, `APPROVES_PHASE: NONE`) | 5735e0bd314314004add920fbb8cf6fd40d43db3 | Second consolidated remediation, closing all findings from independent re-audit `argus-phase-3-remediation-audit-001` (`FAIL_REMEDIATION_REQUIRED`, which found round 1 only PARTIALLY closed the frozen findings). **P3-R6a (new, most severe): migration 0011's original committed form destructively `DELETE`d all 4 derived Phase 3 decision tables and reset `wallets.current_tier = NULL` on every upgrade** -- explicitly prohibited by MASTER_SPEC's append-only-history rule. Fixed by amending the still-UNAPPROVED 0011 migration in place (explicit narrow change-control authorization) to remove every `DELETE`/`UPDATE` statement and make the 3 new provenance columns nullable instead of `NOT NULL`; new migration `0012_phase3_remediation_002_widen_provenance_columns_nullable.py` widens the same columns for a database (this sandbox's own dev DB) that had already run 0011's original destructive form before the fix landed -- this sandbox's disposable local dev database genuinely lost its prior Phase 3 decision rows earlier in this session, disclosed explicitly (checkpoint section C), no other environment affected, no recomputation claimed to restore original beliefs. **P3-R1/P3-R2 continued**: future-economic-timestamp filtering, previously invisible to history assessment and silently applied with no persisted reason, is now ONE shared `qualification_service._filter_swaps_by_as_of()` step feeding history assessment, position reconstruction, and scoring identically, persisting exclusions with reason `FUTURE_ECONOMIC_TIMESTAMP` in a new `wallet_history_quality.excluded_evidence` column; the CLI's arbitrary-JSON `--acquisition-manifest-file` acceptance path is removed entirely, replaced by a real acquisition-execution-and-persistence path (`argus.wallets.acquisition.run_wallet_acquisition`, new `wallet_acquisition_runs` table, migration 0013) with `load_verified_acquisition_manifest` as the ONLY path from a `run_id` to a usable manifest. **P3-R3/P3-R5 continued**: same-slot/same-content sort-key ties gained an immutable `swap_id` final tie-break; currency-valued aggregates (net PnL, profit factor, lottery contribution, drawdown) are now `None` whenever closed positions span more than one distinct quote asset, never silently summed across incompatible units; `max_drawdown` is now `None` (never a crash) whenever any closed position lacks a known `final_exit_at`, with `round_trip_index` added as a stable same-token/same-instant tie-break. **P3-R6b continued**: `wallet_score_snapshots.descriptive_score`/`qualification_score` widened from `Numeric(6,3)` to `Numeric(20,15)` (migration 0014), with the service itself now quantizing the computed score to that exact precision immediately after computation -- BEFORE persistence, the returned result, or the tier decision ever see it, so stored/returned/tier-used are always byte-identical; a new nullable `wallet_score_snapshots.history_id` FK (migration 0015) binds score identity to the exact history/acquisition-manifest row that justified it; the score/history/position idempotency searches were rewritten from "the wallet's latest row by `created_at`" to "search ALL rows matching the relevant scoping key for a full content match" (the `wallet_positions` search fix was found during this round's own adversarial test-writing -- the same bug class, covered by this instruction's own acceptance text, not separately named); the tier-lifecycle write path now computes the FROM-state as "the tier as of `now`" (not the wallet's global-latest tier) and only advances the denormalized `current_tier` cache when `now` is at or after the wallet's own global-latest transition, with an exact-replay guard preventing a duplicate transition. New migrations `0012`-`0015` (widen-nullable, `wallet_acquisition_runs`, lossless score storage, score-history binding). 6 new integration tests in a new `tests/integration/test_wallet_acquisition.py`, 4 new unit tests (P3-R3/P3-R5), 4 new migration tests (P3-R6a), 7 new integration tests (1 P3-R1/P3-R2, 6 P3-R6b) in `test_phase3_wallet_qualification.py`. 759 tests passing (up from 738), ruff+mypy+format clean, migration-from-zero/upgrade-from-already-0011/downgrade-then-reupgrade clean through 0015, 12/12 real-chain fixtures ok, secret scan clean on this round's 20 changed files, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. The accepted `PHASE_3_CANDIDATE_SAMPLE_BLOCKED` result is unchanged, per this instruction's own explicit non-blocking disposition. See `orchestration/checkpoints/phase_3_remediation_2.md` for the full finding-to-evidence disposition; the paired bundle embeds raw command output (not narrative pass-count claims) per this instruction's own E1 requirement; historical `orchestration/checkpoints/phase_3.md`/`phase_3_remediation.md` and their bundles are left byte-for-byte unmodified, preserved as evidence. `last_orchestrator_approved_phase` remains `2` -- this remediation approves no phase. **Phase 3 remains NOT orchestrator-approved.** **Superseded by the remediation round 3 row below** -- an independent focused re-audit (`argus-phase-3-remediation-audit-002`) found one remaining frozen P3-R2 defect; this row is kept unmodified as immutable history. |
| 3 (remediation round 3) | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-3-remediation-003`, target `ad21304a2f9fedd3c11a39a8d840ce577e0afe58`, `APPROVES_PHASE: NONE`) | 34080e5e70f88b668af6ca3543e1d1f39145d582 | Third, narrowly-scoped remediation, closing the one remaining frozen P3-R2 defect from independent focused re-audit `argus-phase-3-remediation-audit-002` (`FAIL_NARROW_REMEDIATION_REQUIRED`): the persisted acquisition manifest was still only a trusted summary assertion -- it stored no run/as-of identity inside itself, no per-address page/transaction counts, no transaction signatures, no chain-event/payload hashes, no parser outcomes, no swap/event references, and no expected-boundary state, and `load_verified_acquisition_manifest` verified only row existence/wallet_id/observation_cutoff before trusting the JSONB blindly -- plus a reproduced `bool("false")`-is-`True` truthiness bug in `manifest_from_dict`. Fixed by extending `AcquisitionManifest` with its own bound `run_id`/`wallet_id`/`wallet_address`/`observation_cutoff`/`algorithm_version` identity, a new `WalkStats` type (status/known_gaps/pages_fetched/signatures_seen/transaction_fetch_failures/expected_oldest_slot/boundary_satisfied) attached to the wallet-address walk and every associated-token-account walk, and a new `acquired_evidence` tuple naming the EXACT signature/slot/chain_event_id/payload_hash/parser_outcome/parser_version/build_hash/derived_swap_id for every signature the run touched; `run_wallet_acquisition` now generates `run_id` first and populates every field from the walk it actually executed, verifying each associated token account's on-chain `owner` actually matches the wallet being acquired (excluding any mismatch from coverage) and treating a parse exception, a per-transaction fetch failure, or an already-known event with no derived swap evidence as an explicit, honestly-named gap that caps history completeness below HIGH rather than ever being "blessed" by mere event existence; `load_verified_acquisition_manifest` now independently re-verifies every `PARSED`/`ALREADY_KNOWN_VERIFIED` evidence entry against the real, current `chain_events`/`swaps` rows before returning the manifest at all. `manifest_from_dict` now requires `token_accounts_enumerated` to be a genuine JSON boolean (never `bool(...)`-coerced) and rejects any unrecognized status/outcome literal or duplicate account/evidence identity outright. `historical_acquisition.AcquisitionResult` gained matching `expected_oldest_slot`/`boundary_satisfied` fields threaded end-to-end as real typed data; `argus wallets acquire-history` gained a matching `--expected-oldest-slot` CLI option. 18 new focused integration tests in `tests/integration/test_wallet_acquisition.py` (6 -> 24) covering fail-closed manifest decoding, exact evidence binding/verification-on-load, every non-HIGH-blessing gap scenario, account-owner mismatch exclusion, and the full boundary-supplied/unsatisfied/satisfied/no-boundary matrix. 777 tests passing (up from 759), ruff+mypy+format clean, no new migration this round (alembic head unchanged at 0015), migration preservation regression tests re-run and passing, 12/12 real-chain fixtures ok, secret scan clean on this round's 7 changed files, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. No previously-closed finding was reopened or reworked; the accepted `PHASE_3_CANDIDATE_SAMPLE_BLOCKED` result is unchanged. See `orchestration/checkpoints/phase_3_remediation_3.md` for the full finding-to-evidence disposition and acceptance-criteria matrix; the paired bundle embeds raw command output verbatim; historical `orchestration/checkpoints/phase_3.md`/`phase_3_remediation.md`/`phase_3_remediation_2.md` and their bundles are left byte-for-byte unmodified, preserved as evidence. `last_orchestrator_approved_phase` remains `2` -- this remediation approves no phase. **Phase 3 remains NOT orchestrator-approved.** **Superseded by the remediation round 4 row below** -- an independent focused re-audit (`argus-phase-3-remediation-audit-003`) found round 3 still `FAIL_REMEDIATION_REQUIRED` on a two-part continuation of the SAME P3-R2 requirement; this row is kept unmodified as immutable history. |
| 3 (remediation round 4) | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-3-remediation-004`, target `fb2a3f7d2b75c526d06568ab3708ff85e1c1448d`, `APPROVES_PHASE: NONE`) | 2cbdb81ca917eac877a9f9f68dfbffc57d69998b | Fourth, consolidated remediation of the SAME P3-R2 evidence-binding requirement, per independent focused re-audit `argus-phase-3-remediation-audit-003` (`FAIL_REMEDIATION_REQUIRED`, two-part justification table). **P3-R2a**: `qualification_service.reconstruct_and_score_wallet`'s `LIVE_ACQUISITION_WALK` branch queried every `Swap` row for `wallet_address`, never restricted to the verified run's own `acquired_evidence` -- a fully-verified run could be "blessed" by unrelated or differently-scoped swap rows already present for the same wallet (adversarial probes 1-2). Fixed by loading the manifest FIRST and restricting the swap query to exactly the manifest's own genuine (`PARSED`/`ALREADY_KNOWN_VERIFIED`) `derived_swap_id` set; a genuinely empty bound set correctly falls through to the pre-existing zero-evidence `UNKNOWN` behavior rather than an unbounded query. **P3-R2b**: `manifest_from_dict` defaulted MISSING `acquired_evidence`/`associated_token_accounts` keys to `[]`; genuine-outcome evidence could carry a null `derived_swap_id` and still load successfully since the loader only re-verified non-null references; `wallet_walk_status` could disagree with the structured `wallet_walk.status`, and a walk could claim `COMPLETE` alongside a recorded fetch failure or unsatisfied boundary (probes 2-4). Fixed by requiring the two array keys to be explicitly present (an explicit empty array remains legitimate), requiring a non-null, real-typed `derived_swap_id`/`parser_version`/`build_hash` for every genuine-outcome evidence entry at decode time, reconciling `wallet_walk_status` against `wallet_walk.status` (and each account's own status against its own walk status), and rejecting `COMPLETE` co-occurring with a fetch failure or an unsatisfied boundary; `load_verified_acquisition_manifest` additionally verifies the manifest's own `wallet_address` against the caller's authoritative wallet row and verifies the referenced swap's actual `parser_version`/`build_hash` match what the evidence entry claims (never merely "a swap for this event exists"). The producer's `ALREADY_KNOWN_VERIFIED` branch now records the pre-existing swap's real historical `parser_version`/`build_hash` instead of null metadata. 12 new focused integration tests in `tests/integration/test_wallet_acquisition.py` (24 -> 36) covering explicit-array presence, null/nonexistent/wrong-event/wrong-artifact derived-swap rejection, and every conflicting-status/COMPLETE-vs-fault case; 3 new full-path integration tests in `tests/integration/test_phase3_wallet_qualification.py` (14 -> 17) proving reconstruction bound only to named evidence (an unrelated real closed position proven absent), empty-evidence retaining `UNKNOWN` despite unrelated real rows, and a two-parser-artifact rebind yielding an honest new history/score identity without rewriting the prior decision. No new migration this round (alembic head unchanged at 0015) -- every change is software wiring within the existing `wallet_acquisition_runs.manifest` JSONB column. 792 tests passing (up from 777), ruff+mypy+format clean, migration preservation regression re-confirmed, 12/12 real-chain fixtures ok, secret scan clean on this round's 5 changed files, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. No previously-closed finding was reopened or reworked; the accepted `PHASE_3_CANDIDATE_SAMPLE_BLOCKED` result is unchanged. See `orchestration/checkpoints/phase_3_remediation_4.md` for the full two-finding closure matrix and acceptance criteria; the paired bundle embeds raw command output verbatim; historical `orchestration/checkpoints/phase_3.md`/`phase_3_remediation.md`/`phase_3_remediation_2.md`/`phase_3_remediation_3.md` and their bundles are left byte-for-byte unmodified, preserved as evidence. `last_orchestrator_approved_phase` remains `2` -- this remediation approves no phase. **Phase 3 remains NOT orchestrator-approved.** |
| 4 | BUILD COMPLETE, AWAITING ORCHESTRATOR REVIEW (authorized by `argus-phase-4-001`, target `379c5bc886abe7e99cdd3360fe3e71925ac932ce`, `APPROVES_PHASE: 3`) | 92bcc6a2d9b77497d077c671df9a5fb6d011332a | Phase 4 (PROSPECTIVE MONITORING + SHADOW COPYING): 5 new tables (migration 0016) -- `prospective_events`, `shadow_intents`, `shadow_positions`, `shadow_quote_probes`, `shadow_mark_outcomes` -- with least-privilege role grants (`argus_executor` gets nothing; no live order/signing/broadcast path exists). A decoupled `argus.shadow.prospective` consumer scans already-persisted Phase 1 `chain_events`/`swaps` evidence (never inlined into `ReconciliationEngine`'s transactional path) to create point-in-time-frozen prospective events for tier-allowed tracked wallets, reusing the SAME `config/signals_v1.yaml` eligibility thresholds that already govern live eligibility elsewhere -- never a manufactured looser bar; `graph_state_snapshot` is always the explicit "Phase 7 not yet implemented" sentinel, never fabricated. Shadow intent/position lifecycle with 6 scheduled entry-delay quote probes (1/5/15/30/60/300s); mark and reverse-executable outcome families (5m/30m/1h/6h/24h horizons) proven distinct for the same position; 5 typed unsellable outcomes (`NO_ROUTE`/`INSUFFICIENT_LIQUIDITY`/`PRICE_IMPACT_EXCESSIVE`/`TOKEN_RESTRICTED`/`PROVIDER_CAPACITY_MISS`) preserved as real, queryable rows, never dropped or replaced with fabricated P&L. Restart-safety (section 84) via the same claim/call/record 3-step shape already proven by Phase 2's `run_archaeology` -- atomic `FOR UPDATE SKIP LOCKED` claim, network call outside any open transaction with actual request/response timestamps captured via an injected `Clock`, one atomic terminal-write transaction guarded by an idempotent already-responded no-op check -- proven via a real crash-injection test and an already-responded reprocessing test, both against real Postgres. Notification-only Telegram integration (`TelegramNotifier`, closed 12-event-type set, secret-shaped-content rejection, `FakeTelegramTransport` used everywhere -- `HttpTelegramTransport` exists as real reviewable code but is never invoked with a real token) and `argus report daily` (every figure a real queried count over the window; `NOT_IMPLEMENTED`/`UNAVAILABLE` sentinels for later-phase or unmeasurable fields, never invented activity) -- all wired through real `argus prospective run`/`argus shadow run-entry-probes`/`argus shadow run-reverse-probes`/`argus shadow run-mark-outcomes`/`argus report daily` CLI commands, not test-only helpers. An integrated production-entry-point REPLAY lifecycle demonstration (`scripts/argus_phase4_replay_demo.py`, `orchestration/phase_4/DEMONSTRATION.md`) proves all eight named steps (leader executes -> ARGUS observes -> source tx confirms -> shadow signal -> entry quote -> shadow fill -> mark outcome -> reverse executable quote) plus the fake notification and daily report through the SAME service functions the real CLI commands call -- "leader executes" grounded in a genuine, independently-verified mainnet pump.fun buy transaction parsed by the real, unmodified generic parser (never a hand-fabricated `Swap` row); entry/reverse quote responses and mark-price snapshots are deterministic injected fakes (no live network/paid-provider access in this sandbox), clearly labeled "REPLAY -- NOT PROSPECTIVE ALPHA EVIDENCE" throughout. All 10 frozen acceptance gates from `argus-phase-4-001`'s own table pass, including "stream gaps block eligible live state" (the unmodified, pre-existing Phase 1 `WalletStreamState.wallet_live_state=DEGRADED` mechanism -- Phase 4 adds no live-entry path at all). 808 tests passing (up from 792; +16 new: 8 integration `test_shadow_phase4.py`, 2 integration `test_daily_report.py`, 6 unit `test_telegram_notifier.py`), ruff+mypy+format clean (128 source files), migration-from-zero/downgrade-0015/upgrade-head round-trip clean through 0016, 12/12 real-chain fixtures ok, secret scan clean on all 25 changed files/directories, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_4.md` for the full 10-gate acceptance disposition. This instruction's own explicit Phase 3 approval is applied above (`last_orchestrator_approved_phase: 3`); **Phase 4 itself remains NOT orchestrator-approved** -- only an independent audit may advance it. **Superseded by the remediation round 1 row below** -- an independent audit (`argus-phase-4-audit-001`) found 7 real defects (`FAIL_REMEDIATION_REQUIRED`); this row is kept unmodified as immutable history. |
| 4 (remediation round 1) | BUILD COMPLETE, AWAITING INDEPENDENT RE-AUDIT (authorized by `argus-phase-4-remediation-001`, target `d95a629985668a0ba73795d3ad8daeb5534ce855`, `APPROVES_PHASE: NONE`) | 285f5a9fe993ff72a02ef6470ea9627952389428 | First consolidated Phase 4 remediation, closing all 7 frozen findings (P4-R1 through P4-R7) from independent audit `argus-phase-4-audit-001` (`FAIL_REMEDIATION_REQUIRED`) as one batch, per this instruction's own explicit ordering (P4-R7 isolation first, then P4-R1-R3 observation/lifecycle, P4-R4-R5 provider/jobs, P4-R6 reporting/notifications). **P4-R1** (future information in the initial prospective snapshot): every point-in-time evidence field a prospective event's frozen snapshot draws from (score, tier, token market state, open-position context, cluster links) is now selected strictly as-of `swap.first_seen_at` (`argus.shadow.prospective`'s `_score_snapshot_as_of`/`_tier_transition_as_of`/cutoff-filtered `_token_state_snapshot`/`_position_size_context`/`_cluster_state_snapshot`), never as-of wall-clock scan time; wallet tier is now resolved from immutable `WalletTierTransition` history, never `wallets.current_tier`; selected source-row identities (`score_snapshot_id`/`tier_transition_id`, migration 0017) are persisted alongside the snapshot so it can be independently checked. **P4-R2** (wrong time origin for delay probes): `_schedule_entry_delay_probes` now anchors every `target_due_at` to `event.first_seen_at`, never to the monitoring pass's own wall-clock `now`; row `created_at` still honestly records real creation time, never backdated. **P4-R3** (consumer stalls; confirmation/replay lifecycle incomplete): the candidate-swap scan now excludes already-claimed economic events via a real `event_id` `NOT EXISTS` check BEFORE any SQL `LIMIT` (a fully-processed batch can no longer permanently starve later genuinely-new swaps), and deduplicates per canonical `chain_events.event_id` via `DISTINCT ON` (two parser artifacts of one transaction can no longer create two shadow trades) -- migration 0017 adds the NOT-NULL, uniquely-constrained `prospective_events.event_id` column this requires, backfilled from `swaps.event_id`; new `revisit_pending_confirmations` safely updates ONLY `confirmation_time` on an already-created event once real confirmation evidence later appears, wired into `run_prospective_monitoring_pass` ahead of the new-event scan every pass. **P4-R4** (provider failures/capacity not reaching honest outcomes): `_classify_provider_exception` now recognizes a real `PriorityScheduler.RequestDropped` (-> `PROVIDER_CAPACITY_MISS`) and real `httpx.HTTPStatusError`s (HTTP 429 -> capacity miss; Jupiter's documented `COULD_NOT_FIND_ANY_ROUTE` error code -> `NO_ROUTE`; anything else honestly `QUOTE_FAILED`, never guessed), not just the fake-provider-only `ShadowQuoteError` family; `_classify_quote` now verifies the response's own `inAmount` matches the requested notional and requires genuine non-empty `routePlan` evidence before ever reporting `route_present=True`/`SUCCESS` (a positive `outAmount` with an empty/absent route is honestly `NO_ROUTE`, never a fabricated fill); the shared `PriorityScheduler` (previously built and unit-tested but never actually instantiated in any production code path) is now wired into the real CLI (`argus shadow run-entry-probes`/`run-reverse-probes`) under dedicated priority classes `P4_prospective_copyability_quote`/`P5_shadow_exit_quote`; `DexScreenerClient` construction now supplies the shared `SqlUsageRecorder` it previously omitted. **Found and fixed beyond the audit's own findings while writing this fix's own required tests**: a parsed-but-nonfinite (`Decimal("NaN")`/`Decimal("Infinity")`) `priceImpactPct` crashed the comparison in `_classify_quote` with an uncaught `decimal.InvalidOperation`, aborting the entire claimed batch rather than degrading to the documented `None`-impact leniency -- fixed by folding a nonfinite parse result into the same honest leniency path immediately after parsing. **P4-R5** (overlapping workers can replace terminal evidence): `shadow_quote_probes`/`shadow_mark_outcomes` gain a `claim_generation` column (migration 0017), incremented atomically on every claim; the terminal-write step now re-reads the row `SELECT ... FOR UPDATE` and verifies the generation this attempt itself observed still matches before publishing, so a stale worker superseded by a fresh reclaim can never overwrite the fresh attempt's result (Postgres row-locking additionally serializes any two workers racing through this step concurrently); an already-terminal invocation now returns immediately BEFORE ever calling the provider/market-data client, closing a "needless provider call on an already-completed probe" gap the existing no-op regression test had been silently masking (strengthened with an explicit provider-call counter). **Found and fixed beyond the audit's own findings while writing this fix's own required tests**: two different entry-delay probes for the SAME shadow intent could both resolve `SUCCESS` and race, under genuine concurrent load, to create the first `ShadowPosition` for that intent via an unguarded check-then-insert, raising an unhandled `IntegrityError` on the position's own unique `shadow_intent_id` constraint -- fixed by acquiring a `SELECT ... FOR UPDATE` lock on the parent `ShadowIntent` row before the `existing_position` check, serializing concurrent creators (the loser blocks on the lock, then its own post-lock re-check correctly finds the winner's already-committed row). **P4-R6** (report tier-direction/new-wallet counting wrong; notifier disconnected): `reports/daily.py`'s promotion/demotion counting now compares `from_tier`/`to_tier` by real `WALLET_TIERS` rank order (S->A is correctly a demotion, DISCOVERED->WATCH and PROBATION->B are correctly promotions -- the audit's own worked example, previously miscounted "1 promotion, 2 demotions", now correctly counts "2 promotions, 1 demotion"), excluding QUARANTINE/DORMANT/RETIRED from the normal progression comparison; `new_wallets` now counts distinct `Wallet.first_discovered_at` identities in the window, never repeated `wallet_discovery_events` rows for an already-known wallet; previously-`NOT_IMPLEMENTED` fields this offline report can genuinely answer from already-persisted evidence (`low_completeness_wallets`, `provider_gaps`, descriptive `mfe_mae`, research `sample_counts`) are now populated from real queries; an injectable, best-effort, `contextlib.suppress`-guarded `notifier` parameter is now threaded through BOTH `build_daily_report` (one real `DAILY_SUMMARY` notification after the report is built) and `argus.shadow.quote_jobs`'s entry-probe fill path (one real `SHADOW_EVENT` notification on a genuine new position), defaulting to no-op, `FakeTelegramTransport` in tests/the REPLAY demo -- closing the "a working notifier class exists but no ordinary production service ever calls it" gap; a notification failure never affects the underlying committed record. **P4-R7** (replay demo could delete unrelated history and consume unrelated jobs): `scripts/argus_phase4_replay_demo.py` no longer runs against the shared configured database or does address-keyed cleanup; it now follows the same disposable-scratch-database pattern `tests/integration/test_migrations.py` already established -- creates a uniquely-named scratch database, points `ARGUS_DB_NAME` at it for the run's duration (migrations run via `asyncio.to_thread` to avoid alembic's own nested-event-loop conflict), runs the full lifecycle, unconditionally `DROP DATABASE`s it afterward -- with a defense-in-depth `refuse_unless_scratch_database` guard checked before every write/network-shaped call; a pre-existing shared-database wallet with unrelated queued due jobs is proven byte-for-byte untouched across a clean run and fault injection at all 4 lifecycle points (before/during/after setup, before lifecycle). Additive migration `0018_phase4_remediation_1_confirmation_time_update_grant.py` grants `argus_ingest` `UPDATE` on ONLY `prospective_events.confirmation_time` (column-level, not the whole row) -- a genuine gap discovered while writing P4-R3's own focused tests: `revisit_pending_confirmations`'s real `UPDATE` had no granted privilege at all under the role every production writer actually connects as, so a genuinely late-arriving confirmation would have thrown `InsufficientPrivilegeError` in real production despite the Python-level fix being correct; every other column on this table remains as strictly append-only as migration 0016 intended. 51 new focused tests across 5 new files (12 `test_shadow_phase4_remediation_observation.py` for P4-R1/R2/R3, 14 `test_shadow_quote_jobs_provider_remediation.py` for P4-R4 against a REAL `JupiterClient`+`httpx.MockTransport`+real `PriorityScheduler`, 7 `test_shadow_phase4_concurrency_remediation.py` for P4-R5 with genuine `asyncio.gather` interleaving, 10 `test_daily_report_remediation.py` for P4-R6, 8 `test_replay_demo_isolation.py` for P4-R7), plus the existing no-op regression test in `test_shadow_phase4.py` strengthened with a provider-call counter and `test_migrations.py`'s 9 hardcoded head-revision assertions bumped `0016`->`0018`. 859 tests passing (up from 808; +51 exactly matches the 5 new test files), ruff+mypy+format clean (unchanged file count), migration-from-zero/downgrade-0016/upgrade-head round-trip clean through 0018, 12/12 real-chain fixtures ok, secret scan clean on all 23 changed/new files, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_4_remediation_1.md` for the full 7-finding closure matrix (plus the two genuine gaps found and fixed beyond the audit's own list, disclosed above and in the checkpoint). `last_orchestrator_approved_phase` remains `3` -- this remediation approves no phase. **Phase 4 remains NOT orchestrator-approved.** **Superseded by the remediation round 2 row below** -- an independent re-audit (`argus-phase-4-remediation-audit-002`) found round 1 only PARTIALLY sufficient on 4 of the 7 findings (P4-R1, P4-R3, P4-R4, P4-R6); this row is kept unmodified as immutable history. |
| 4 (remediation round 2) | BUILD COMPLETE, AWAITING INDEPENDENT RE-AUDIT (authorized by `argus-phase-4-remediation-002`, target `1d5cc5d93819cdeec050889a5b37c44d5b2f5c0b`, `APPROVES_PHASE: NONE`) | 9890802f91da02c51fc4a2f12715c821158dc53b | Second, narrowly-scoped Phase 4 remediation, closing the 4 frozen findings (P4-R1, P4-R3, P4-R4, P4-R6) an independent re-audit (`argus-phase-4-remediation-audit-002`, `FAIL_REMEDIATION_REQUIRED` with a concrete adversarial-probe justification table) found only PARTIALLY closed by round 1; P4-R2/P4-R5/P4-R7 are confirmed CLOSED per this instruction's own explicit disposition and were not reopened or reworked. **P4-R1 continued** (first-seen knowledge boundary): point-in-time evidence selection now enforces BOTH an effective-time bound AND a recorded-time bound together (`_score_snapshot_as_of`/`_tier_transition_as_of` require `as_of <= cutoff AND created_at <= cutoff` / `transitioned_at <= cutoff AND created_at <= cutoff` -- round 1 only bounded one side, so a score/transition effective-in-the-past but not-yet-recorded-until-later leaked through); `_token_state_snapshot` no longer falls back to the token's denormalized CURRENT `current_lifecycle_stage` when no point-in-time market snapshot exists as-of cutoff (an explicit `lifecycle_stage: None` with a named reason instead), and excludes tokens not yet first-observed as of cutoff entirely; most severely, `scan_for_new_prospective_events`'s own SCANNER-level candidate-selection gate used the wallet's CURRENT tier (`Wallet.current_tier.in_(tier_allowed)`) even though the snapshot-content logic already correctly used point-in-time tier history -- a later promotion could retroactively pull an old, actually-ineligible swap into the candidate set (with a correctly-historical but wrongly-included snapshot), and a later demotion could permanently exclude an old, genuinely-eligible swap; fixed via a correlated SQL subquery computing each swap's own tier-at-its-own-cutoff, applied inside the same LIMIT-bounded query (never a Python-side post-filter, which would reintroduce starvation). **P4-R3 continued** (confirmation and repeated-pass lifecycle): confirmation lookups in both `_create_prospective_event` and `revisit_pending_confirmations` checked only `commitment_level == CONFIRMED` with no `transaction_succeeded` filter at all -- a CONFIRMED observation of a FAILED transaction was silently treated as a successful confirmation, and a FINALIZED-only success was never considered; fixed via one new shared `_confirmed_success_observation` helper (both call sites now agree byte-for-byte) requiring `transaction_succeeded IS TRUE` and accepting either CONFIRMED or FINALIZED, selecting the earliest qualifying observation by `(observed_at, sequence)`; `revisit_pending_confirmations` selected its candidate batch purely by `confirmation_time IS NULL` with no check that resolvable evidence actually existed yet, so permanently-unconfirmable old rows could occupy every `LIMIT` slot forever and starve a later genuinely-resolvable row -- fixed via an `exists()` subquery for resolvable evidence added to the candidate WHERE clause, before `LIMIT`; `scan_for_new_prospective_events`'s per-candidate creation loop had no protection against a concurrent scanner's TOCTOU race on its own NOT-EXISTS check, so a real unique-constraint `IntegrityError` from a losing concurrent insert propagated uncaught and aborted the entire batch including unrelated candidates -- fixed by wrapping each candidate's creation in a Postgres SAVEPOINT (`session.begin_nested()`) and treating a resulting `IntegrityError` as an idempotent no-op, proven via genuine `asyncio.gather` concurrency (not sequential replay). **P4-R4 continued** (honest provider evidence): `_classify_quote` now checks the response's own RAW `inputMint`/`outputMint` fields against the actual request, never the caller-echoed labels `ExecutableQuote` merely restates from the request (round 1's fix only checked `inAmount`, missing a genuine provider-side mint disagreement entirely); route-plan evidence now requires each entry to be structurally well-formed (its own `swapInfo` object), not merely a non-empty list (`routePlan=[null]` is equivalent to no route); a SUPPLIED-but-garbage/nonfinite `priceImpactPct` is now an explicit `QUOTE_FAILED` distinct from a genuinely ABSENT field (which stays leniently `None`) -- a deliberate reversal of round 1's own "fold nonfinite into the same leniency as missing" fix, which the round-2 audit correctly identified as conflating two different evidentiary states; Jupiter's own structured `platformFee.amount` is now honestly parsed into `fee_estimate_raw` when present and well-formed, `None` otherwise, never fabricated or summed across currencies; `requested_at`/`responded_at` are now captured from INSIDE the actually-dispatched callable (via `nonlocal` bindings set at real dispatch/response time), so a scheduler queue wait before dispatch is correctly reflected in `scheduling_delay_seconds` rather than conflated with post-dispatch network latency -- proven via a real `PriorityScheduler` genuinely holding a submission queued behind a blocker until released, with a scripted clock supplying the worked example's exact T+60/T+60.1 instants; a new `shadow_quote_probes.terminal_at` column (migration 0020) is set on EVERY terminal write regardless of whether a real dispatch happened, decoupling "is this probe done" (claim-candidate filtering, `_maybe_finalize_intent_no_fill`, and `reports/daily.py`'s report-window queries) from `responded_at IS NOT NULL`, which a genuine scheduler-level `PROVIDER_CAPACITY_MISS` drop never sets at all. **P4-R6 continued** (current-phase report data): SHADOW's descriptive `mfe_mae` now comes from this window's own real `ShadowMarkOutcome` returns (sampled max/min with a count and an explicit sampled-not-continuous caveat), never the historical Phase 3 `WalletPosition` figures round 1 substituted in their place; those historical figures, when retained, now live in RESEARCH's own separately labeled `historical_backtest` section, grouped by `quote_asset_mint` (never averaged across e.g. SOL and USDC) and restricted to each wallet's CURRENT chosen history reconstruction (a `DISTINCT ON` latest-`WalletHistoryQuality`-row-per-wallet subquery) so a superseded/replayed reconstruction's positions never inflate the sample; `data_quality["low_completeness_wallets"]` now counts distinct wallets by their CURRENT (latest) assessment only, never every historical LOW/UNKNOWN row (a wallet reassessed LOW->LOW->HIGH now correctly counts 0, not 2); the former single `matured_executable_outcomes_in_window` count, which mixed successful/unsellable/missing-capacity REVERSE_EXECUTABLE outcomes together and invited being read as a usable executable sample size, is replaced by an explicit `reverse_executable_outcomes_in_window` successful/unsellable/missing_capacity breakdown with `usable_sample` excluding missing-capacity terminal no-send records, plus a separate `reverse_executable_overdue_unattempted` count naming probes past due but not yet terminal at all, kept distinguishable from a terminal no-send capacity miss. Two new additive migrations: `0019_phase4_remediation_2_confirmation_observation_link.py` (nullable `prospective_events.confirmation_observation_id` FK to `commitment_observations`, plus a column-scoped `GRANT UPDATE` for `argus_ingest`, following the same narrow-grant pattern as migration 0018) and `0020_phase4_remediation_2_probe_terminal_at.py` (nullable `shadow_quote_probes.terminal_at`, plus a `CHECK (responded_at IS NULL OR terminal_at IS NOT NULL)` constraint). `scripts/argus_phase4_replay_demo.py`'s evidence output directory was moved to `orchestration/phase_4_remediation_2/evidence/` for this round (round 1's own frozen `orchestration/phase_4_remediation_1/evidence/replay_demo_results.json` is preserved byte-for-byte unmodified). 31 new/extended focused tests across the existing round-1 files (`test_shadow_phase4_remediation_observation.py` 12->31 covering P4-R1/P4-R3 continued; `test_shadow_quote_jobs_provider_remediation.py` 14->24 covering P4-R4 continued, including a genuine controlled-real-scheduler timing test and an exact-replay-zero-further-calls assertion on the capacity-drop path; `test_daily_report_remediation.py` 10->12, replacing the two test oracles the round-2 audit found were duplicating production's own formula/mixing historical data into shadow figures with independent hand-computed oracles matching the instruction's own worked examples), plus `test_shadow_phase4.py`/`test_replay_demo_isolation.py` fixture updates (added scripted-clock values for the new `terminal_at` call, `inputMint`/`outputMint` fields in fake quote raw payloads) required by the above source changes but not new coverage themselves. 890 tests passing (up from 888 after the round-1 baseline of 859 plus round-1's own subsequent fixes; net +31 exactly matches the extended/new test count above), ruff+mypy+format clean (128 source files), migration downgrade-0018/upgrade-head round-trip clean through 0019/0020 (re-verified after the full suite ran), 12/12 real-chain fixtures ok, secret scan clean on this round's 15 changed/new files plus the new `orchestration/phase_4_remediation_2/` evidence directory, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_4_remediation_2.md` for the full requirement-to-evidence closure matrix. `last_orchestrator_approved_phase` remains `3` -- this remediation approves no phase. **Phase 4 remains NOT orchestrator-approved.** |
| 4 (root-cause recovery) | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-4-recovery-001`, target `9aa8b8decf8cb17e1b3bb28e9e1ebd0b2083acda`, `APPROVES_PHASE: NONE`) | f932ce1a61358fd5bbdcc4fe7fcf64ff777a35ac | A controlled recovery from the explicit STOP an independent audit (`argus-phase-4-failure-review-001`, `FAIL_STOP_ROOT_CAUSE_REVIEW`) issued after finding remediation round 2 only PARTIALLY sufficient -- 5 SPEC_BLOCKING findings (R1-T, R4-V, R4-E, R4-M, R6-T) frozen for exactly this recovery; every other previously-PASS/CLOSED finding (P4-R2, P4-R3, P4-R5 worker ownership, P4-R7, and every already-closed round-2 finding) is confirmed unchanged and was not reopened or reworked. **P4-REC-01** (token/position creation-time cutoffs): `_token_state_snapshot` now also bounds `Token.created_at <= cutoff` (previously only `first_observed_at`), and `_position_size_context` now also bounds `WalletPosition.created_at <= cutoff` (previously only `first_entry_at`) -- a Token/WalletPosition row backdated to an effective time at or before cutoff but not actually PERSISTED (created_at) until after cutoff was, before this fix, still treated as known at cutoff; already-passing score/tier/market/cluster dual-clock behavior and the single-history position rule are unchanged. **P4-REC-02** (structural quote-route validation): `_is_structurally_valid_route_entry` now checks the route entry's own `swapInfo` object for genuinely present, correctly-typed, positive `inputMint`/`outputMint`/`inAmount`/`outAmount` fields, not merely that `swapInfo` is a dict -- `swapInfo={}` or a `swapInfo` missing/mistyping any of those four fields previously still counted as SUCCESS route evidence; malformed/wrong-type/missing/non-positive fields now correctly classify as `QUOTE_FAILED`/`NO_ROUTE`, never SUCCESS, and never create a `ShadowPosition`. Three fake test-fixture `_quote()` helpers (`test_shadow_phase4.py`, `test_shadow_phase4_concurrency_remediation.py`, `test_shadow_phase4_remediation_observation.py`) and the REPLAY demo script's own fake quote (`scripts/argus_phase4_replay_demo.py`) had to be updated to supply genuinely complete `swapInfo` objects to keep representing a real SUCCESS route under the deepened contract -- their own SUCCESS-path scenarios were unaffected in intent, only their fixture's structural completeness. **P4-REC-03** (preserve sanitized terminal failure evidence): a new nullable `shadow_quote_probes.failure_evidence` JSONB column (migration 0021) is now populated by `_classify_provider_exception` (which now returns `(outcome, failure_evidence)` instead of `outcome` alone) with a small, bounded, already-sanitized set of keys -- `http_status_code`, `provider_error_code` (capped at 128 chars, real string only), `scheduler_drop_reason`/`scheduler_priority_class` -- never the raw response body, headers, or request URL, and never a fabricated mapping for an unrecognized exception. **P4-REC-04** (populated predecessor migration compatibility, most severe): migration 0020's own `CHECK (responded_at IS NULL OR terminal_at IS NOT NULL)` constraint was validated against every existing row the instant it was created, with no backfill step -- on a real populated database, every pre-existing row with a real provider response already recorded (`responded_at IS NOT NULL`) had no `terminal_at` value yet (that column did not exist before this migration), so the constraint would fail the migration outright. Fixed by amending the still-UNAPPROVED 0020 migration in place (same narrow-change-control precedent as Phase 3's 0011/0012) to backfill `terminal_at = responded_at` for exactly the rows where `responded_at IS NOT NULL AND terminal_at IS NULL`, BEFORE the CHECK constraint is created -- every prior version of this project's code performed its terminal write in the same atomic transaction immediately after computing `responded_at`, so `responded_at` is itself the truthful, deterministically-derived terminal moment for these legacy rows, never a fabricated/current-wall-clock value; a row with `responded_at IS NULL` (still pending, or a genuine scheduler-level capacity drop) already satisfies the constraint via its own left-hand side and is left untouched, so it stays claimable/runnable. Proven against a REAL populated database (schema created at predecessor revision 0018 with completed-success/completed-error/completed-capacity-miss/pending fixture rows, then upgraded through head), not merely a fresh empty schema. **P4-REC-05** (report-end-bounded latest history): `_latest_history_id_per_wallet_subquery` now accepts an optional `cutoff` parameter that bounds candidate rows to `WalletHistoryQuality.created_at <= cutoff` before the existing per-wallet `DISTINCT ON` deduplication runs (unchanged); `_build_data_quality` now passes its own `end` as that cutoff, so a history row created after an earlier report's own `end` can no longer count as "current" in that earlier report merely because it is the globally-latest row by the time the query happens to run. `_build_research`'s own unbounded historical-backtest use (`cutoff=None`, out of this recovery's frozen scope) is unchanged. Every focused test in this round drives the real production functions against real Postgres (never a duplicated test-only reimplementation of the query/validator logic); the report-end-cutoff tests specifically use an independent correlated-`NOT EXISTS` SQL oracle, a genuinely different shape from production's own `DISTINCT ON`, per the audit's own "never duplicate production's formula in the test" finding. New migration `0021_phase4_recovery_probe_failure_evidence.py` (additive-only, nullable `failure_evidence` JSONB column, no new grant needed). `scripts/argus_phase4_replay_demo.py`'s evidence output directory moved to `orchestration/phase_4_recovery/evidence/` for this round (round 2's own frozen `orchestration/phase_4_remediation_2/evidence/replay_demo_results.json` is preserved byte-for-byte unmodified). 911 tests passing (verified via a fresh whole-repository `uv run pytest -q` run at the end of this round: 660 unit/golden/phase_1_5 + 10 replay + 241 integration), ruff+mypy+format clean across the full repository (128 source files), migration graph single head at `0021` with a clean linear `<base>..0021` chain, `alembic upgrade head`/`downgrade -1`/`upgrade head` round-trip clean, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_4_recovery.md` for the full row-by-row acceptance matrix. `last_orchestrator_approved_phase` remains `3` -- this recovery approves no phase. **Phase 4 remains NOT orchestrator-approved.** **Superseded by the recovery round 2 row below** -- an independent audit (`argus-phase-4-recovery-review-001`) confirmed P4-REC-01/04/05 CLOSED but found P4-REC-02/P4-REC-03 only partially closed (3 concrete named defects) plus a new checkpoint-format regression; this row is kept unmodified as immutable history. |
| 4 (recovery round 2) | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-4-recovery-002`, target `e2b0edce094f51b329372ccfb0015fece0103033`, `APPROVES_PHASE: NONE`) | a50432946b5ddeede55f84d61c93375047c564df | Bounded root-cause recovery closing exactly the 3 findings (F-01, F-02, F-03) `argus-phase-4-recovery-review-001` froze after auditing recovery round 1; P4-REC-01/04/05 confirmed CLOSED unchanged, not reopened. **F-01** (`src/argus/shadow/quote_jobs._is_positive_raw_amount`, SPEC_BLOCKING): `str.isdigit()` alone accepts non-ASCII Unicode "digit" characters (e.g. superscript-two `"²"`) that `int()` cannot parse, and Python's own global integer-string-conversion length guard raises `ValueError` for an excessively long (thousands-of-digits) all-ASCII digit string -- both previously escaped this function uncaught, crashing the whole probe-processing call (`_classify_quote` runs in the provider try/except's `else` branch, which is NOT covered by that except) instead of recording an honest terminal `NO_ROUTE`. Fixed by requiring `value.isascii() and value.isdigit()` before ever calling `int()`, then wrapping that conversion in a `ValueError`/`OverflowError` guard returning `False` on failure -- Python's own conversion-length limit is left completely untouched (never raised via `sys.set_int_max_str_digits`), proven by an explicit regression asserting the limit is still the Python default (4300) after this fix. **F-02** (`_safe_provider_error_code`/`_classify_provider_exception`, SAFETY_OR_INTEGRITY_BLOCKING): the provider-error-code sanitizer previously checked only type/length (a non-empty string <=128 chars), so an unsafe-shaped string (a URL with an embedded fake API key, a bare `key=value` query fragment, embedded control characters, JSON-body-shaped text) that happened to fit the length bound was persisted verbatim into `failure_evidence`; separately, the HTTP 429 branch returned immediately without ever inspecting the response body, so a genuinely-supplied safe `errorCode` was silently dropped on a 429 even though every other status preserved it. Fixed by replacing the sanitizer with a bounded ASCII identifier grammar (`^[A-Za-z][A-Za-z0-9_]{0,127}$`, a full-match regex -- never a substring/prefix check), and by restructuring `_classify_provider_exception` to parse the response body and extract a sanitized `provider_error_code` BEFORE branching on status code, so a JSON-parse failure can never erase the 429 capacity classification and a genuinely-supplied safe code now survives on 429 exactly like every other status; 429 itself is still always `PROVIDER_CAPACITY_MISS` regardless of what code is or isn't present. **F-03** (checkpoint/bundle artifact validation, SPEC_BLOCKING): the previous round's own submitted checkpoint (`orchestration/checkpoints/phase_4_recovery.md`, left byte-for-byte unmodified as historical evidence) was never actually run through this project's own production `validate_checkpoint_content`/`validate_bundle_content` functions before submission -- it is missing the required terminal end marker entirely and has no section containing the literal (case-insensitive) required phrase "acceptance criteria" (its own section was titled "Row-by-row acceptance matrix"). This round's own checkpoint/bundle (`orchestration/checkpoints/phase_4_recovery_2.md`/`orchestration/bundles/phase_4_recovery_2.txt`) were mechanically validated against BOTH real, unmodified production validator functions after hash-fill, with the actual `(ok, reason)` tuples (both `(True, '')`) recorded verbatim in the checkpoint's own commands-run section -- calling the functions without asserting `ok` is explicitly not validation. Two new focused test files: `tests/unit/test_phase4_recovery_2_contract.py` (52 tests -- the pure parser/artifact rows AM-01/02/04/08/09/12/13, no database) and `tests/integration/test_phase4_recovery_2.py` (16 tests -- the worker/persistence rows AM-01/03/05/06/07/10/11, real `JupiterClient`+`httpx.MockTransport`+real claim/execute/record seam, entry AND reverse probe kinds). Every counterexample was run FIRST against the unmodified pre-fix code and its honest failing output preserved (never `xfail`) in `orchestration/phase_4_recovery_2/evidence/pre_fix_unit_contract_red.txt` (15 failed, 36 passed, 1 skipped) and `.../pre_fix_integration_worker_red.txt` (7 failed, 9 passed) before either fix was implemented. `scripts/argus_phase4_replay_demo.py`'s evidence output directory moved to `orchestration/phase_4_recovery_2/evidence/` for this round (round 1's own frozen `orchestration/phase_4_recovery/evidence/replay_demo_results.json` is preserved byte-for-byte unmodified -- an accidental regeneration during this round's own full-suite run was caught via `git status` before staging and reverted). No schema migration needed this round (the `failure_evidence` JSONB column already exists from P4-REC-03's migration 0021; `alembic heads` unchanged at `0021`). 978 tests passing (up from 911; +67 exactly matches the 2 new test files, net of 1 order-dependent AM-12 skip resolved by the direct post-hash-fill validator invocation), ruff+mypy+format clean across the full repository, migration graph unchanged single head at `0021`, 12/12 real-chain fixtures ok, secret scan clean on this round's 9 changed/new files plus the new `orchestration/phase_4_recovery_2/` evidence directory, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_4_recovery_2.md` for the full row-by-row acceptance matrix. `last_orchestrator_approved_phase` remains `3` -- this recovery approves no phase. **Phase 4 remains NOT orchestrator-approved.** **Superseded by the recovery round 3 row below** -- an independent audit (embedded directly in `argus-phase-4-recovery-003`) confirmed F-01/F-02/F-03 genuinely CLOSED but found the submitted tests narrower than recovery-002's own frozen acceptance matrix required (COV-01: helper-only/representative coverage substituted for the required Cartesian worker/persistence/reload proof); this row is kept unmodified as immutable history. |
| 4 (recovery round 3) | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-4-recovery-003`, target `87e8ba1b5a7969e5afe4a7e1e6c44eb392365f16`, `APPROVES_PHASE: NONE`) | 75e9ece07aa475e1ffc2413d110f5f0ee88f3134 | TEST-AND-EVIDENCE-ONLY completion round closing the single consolidated finding COV-01 an independent audit found after re-confirming F-01/F-02/F-03 genuinely correct (a fresh scratch harness of 118 real `JupiterClient`/`httpx.MockTransport` cases through the same production common executor, plus both final artifact validators returning `(True, '')`) -- recovery-002's own submitted tests satisfied only a representative subset of its own frozen matrix rows AM-01/02/03/04/08/09/10 (e.g. one hard-coded reverse/outAmount case instead of the full kind x field x value Cartesian; one entry-only reload instead of reload coverage for every malformed-value combination; helper-only boolean/string checks standing in for real persisted-executor proof). **No production code in `src/` is touched this round** -- `git diff --stat src/` is empty; F-01, F-02 and F-03 are not reopened or reworked. Adds `tests/integration/test_phase4_recovery_3_matrix.py` (94 new tests, TC-01 through TC-05, using the real `_execute_and_record_probe` common executor recovery-002's own instruction already permitted directly): **TC-01** (AM-01/02/03, 8 cases: both probe kinds x both nested fields x {non-ASCII superscript-two, 5000-ASCII-digit string}) -- each case a real persisted terminal `NO_ROUTE` with `requested_at<=responded_at<=terminal_at`, zero new `ShadowPosition`, exactly one HTTP call, PLUS a fresh-session reload and repeat-processing pass proving the complete persisted record (outcome/clocks/evidence/generation) is byte-for-byte unchanged and no second HTTP request occurs. **TC-02** (AM-04, 26 cases: nested field x {3 valid, 10 invalid} representations, entry kind only per the frozen row's own common-seam exemption) -- valid representations (`"1"`/`"001"`/int `1`) reach real `SUCCESS` with a genuine new `ShadowPosition`; every invalid shape (empty/garbage/zero/negative/bool/float/None/non-ASCII-digit) reaches terminal `NO_ROUTE` with zero new positions. **TC-03** (AM-05/07/10, 8 cases: both kinds x {429/known-code, 400/known-no-route-code, 400/unknown-safe-code, 429/no-route-shaped-code}) -- exact outcome/evidence per case plus the same reload/repeat identity proof as TC-01. **TC-04** (AM-08/10, 44 cases: both kinds x HTTP400/429 x 11 frozen unsafe-code shapes, each response body/headers also carrying ignored fake-secret sibling fields) -- `provider_error_code` absent in every case, only `http_status_code` persisted, no injected secret/body/header/URL material present in the persisted evidence, plus the same reload/repeat identity proof. **TC-05** (AM-09, 8 cases: HTTP400/429 x {1-char, 128-char, digits/underscore identifier, 129-char-rejected} boundary, entry kind only per the frozen row's own common-seam exemption) -- exact code preserved at/under the 128-char boundary, 129 chars rejected with status-only evidence, no trimming/normalization. A collected-case-inventory cross-check (`--collect-only -q`, 94 nodes) confirms every row's own frozen Cartesian size was actually met, not merely an AM-linked test-node label (this round's own root-cause remedy for why recovery-002's overclaim was possible). `scripts/argus_phase4_replay_demo.py`'s evidence output directory moved to `orchestration/phase_4_recovery_3/evidence/` for this round (round 2's own frozen `orchestration/phase_4_recovery_2/evidence/replay_demo_results.json` is preserved byte-for-byte unmodified), the same narrow evidence-destination-only allowance recovery-002 itself used. No schema migration needed this round (`alembic heads` unchanged at `0021`). 1073 tests passing (up from 979; +94 exactly matches the new test file), ruff+mypy+format clean across the full repository (one self-inflicted import-sort/format issue in this round's own new test file caught and fixed during the acceptance-command sequence itself), migration graph unchanged single head at `0021`, 12/12 real-chain fixtures ok, secret scan clean on this round's 3 changed/new paths, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_4_recovery_3.md` for the full row-by-row acceptance matrix, including the terminology correction distinguishing an instruction's own commit from that instruction's own `TARGET_COMMIT` field value (a documentation-only correction this round's own audit requested; the historical `phase_4_recovery_2.md` checkpoint is left byte-for-byte unmodified). `last_orchestrator_approved_phase` remains `3` -- this round approves no phase. **Phase 4 remains NOT orchestrator-approved.** **Superseded by the recovery round 5 row below** (an intervening recovery-004 instruction, commit `78b70d151217a84227d8c0363340ffed50149457`, was itself superseded by recovery-005 before this session ever executed it -- no evidence exists for round 4 and none is claimed) -- an independent audit embedded in `argus-phase-4-recovery-005` confirmed F-01/F-02/F-03/COV-01 genuinely closed but sealed two additional frozen assertions (ASSERT-01, ASSERT-02) as the complete remaining blocking contract; this row is kept unmodified as immutable history. |
| 4 (recovery round 5) | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-4-recovery-005`, target `c0b774f5deb9898bb6e1cfa4f364a1b458242610`, `APPROVES_PHASE: NONE`) | a6922ac8df812f48a34ff637ddc17e45f3c5afab | SEALED two-assertion completion round, per the new `orchestration/AUDITOR_POLICY.md` governance contract (added by the orchestrator between rounds 3 and 5, additive to MASTER_SPEC.md, mandatory reading before self-audit/independent audit). **No production code in `src/` is touched this round** -- `git diff --stat src/` is empty; F-01/F-02/F-03/COV-01 are not reopened or reworked, confirmed already CLOSED by this instruction's own text. **ASSERT-01** (unchanged scoped probe/position counts, SPEC_BLOCKING if failed): `tests/integration/test_phase4_recovery_3_matrix.py`'s shared `_process_and_reprocess` helper (used by TC-01/03/04's 60 non-SUCCESS cases) now also observes the scoped `ShadowQuoteProbe` row count (by the claimed probe's own parent -- `shadow_intent_id` for ENTRY_DELAY, `shadow_position_id` for REVERSE_EXECUTABLE, via a new `_scoped_probe_count` helper) and the seeded-wallet `ShadowPosition` count at three points -- before first execution, after the committed terminal result, and after the fresh-session repeat -- asserting all three observations are identical; never applied to TC-02's own SUCCESS cases, per the frozen row's own explicit exclusion. `_seed_and_claim_entry`/`_seed_and_claim_reverse` now additionally return the claimed probe's own `shadow_intent_id`/`shadow_position_id` (previously only `wallet_id` was threaded through) so every TC-01/03/04 call site can pass the correct scope. **ASSERT-02** (captured-log absence for unsafe TC-04 values, SPEC_BLOCKING if failed): TC-04's own 44 cases now wrap both executor calls (first execution + fresh-session repeat, both already inside `_process_and_reprocess`) in `caplog.at_level(logging.DEBUG)` and assert that none of the four injected inert fake-secret sentinels, nor the case's own unsafe `errorCode` value itself (in both raw and escaped-`repr()` form, for nonempty string values), ever appear in the captured formatted log text. Root-caused via direct source inspection (`grep` across `quote_jobs.py`, the Jupiter client, retry, and usage-recording modules): this call path emits zero log records today, so no production logger change was needed or made -- the frozen test proves the existing, already-silent behavior, exactly per the instruction's own "if current production passes, make test/evidence changes only" clause. The existing 94-case inventory is byte-identical (verified via `--collect-only -q` cross-check) -- no case family was added or removed, only in-place assertion strengthening. An accidental regeneration of round 3's own frozen `orchestration/phase_4_recovery_3/evidence/replay_demo_results.json`, caused by running the targeted regression suite before moving `scripts/argus_phase4_replay_demo.py`'s `EVIDENCE_DIR` to this round's own new `orchestration/phase_4_recovery_5/evidence` path, was caught via `git status` before staging and reverted with `git checkout --`; the move was then completed before any further evidence-generating command ran, and this exact recurring mistake category is now flagged for future rounds to perform first, before any test command. No schema migration needed this round (`alembic heads` unchanged at `0021`). 1073 tests passing (identical total to round 3 -- zero new test nodes, only strengthened assertions inside existing ones), ruff+mypy+format clean across the full repository (one self-inflicted format issue in this round's own edited test file caught and fixed during the acceptance-command sequence itself), migration graph unchanged single head at `0021`, 12/12 real-chain fixtures ok, secret scan clean on this round's changed/new paths, no credential entered/disclosed, no signing/execution/broadcast/paid-provider path. See `orchestration/checkpoints/phase_4_recovery_5.md` for the full builder self-audit two-row ASSERT matrix. `last_orchestrator_approved_phase` remains `3` -- this round approves no phase. **Phase 4 remains NOT orchestrator-approved.** |
| 5 | BUILD COMPLETE, AWAITING INDEPENDENT AUDIT (authorized by `argus-phase-5-001`, target `354ed229eb4ba8c16622b008b7494b3687da525e`, `APPROVES_PHASE: NONE` for this row's own submission -- Phase 4 was approved by `argus-phase-5-001` itself, see `last_orchestrator_approved_phase` above) | aac910cee873851f266d6d98eb60d90c4be3d49a | First Phase 5 (COPYABILITY + FORWARD INFORMATION VALUE, MASTER_SPEC.md sections 46-53) submission: implements mechanics M1-M7 (`src/argus/copyability/{identity,executable_returns,delay_curves,size_surprise,util,loaders,persistence,service}.py`, `src/argus/scoring/{copyability,readiness,config_weights}.py`), two new additive-only, append-only, idempotently-identified analytical tables (migration `0022`: `wallet_copyability_snapshots`, `opportunity_readiness_snapshots`), the `argus copyability report` CLI command, and P5-11's replay-script `--output-dir` refactor consuming the recovery-005 carryforward CF-P4-01. Uses PRECISELY the existing `copyability_weights`/`trade_readiness_weights` from `config/signals_v1.yaml` -- no retuning. 101 new test nodes across `tests/unit/test_phase5_p5_{01..08,11}_*.py` and `tests/integration/test_phase5_persistence_and_report.py`, mapped one-to-one to the sealed 14-row acceptance contract (P5-01 through P5-14) -- see `orchestration/checkpoints/phase_5.md` section D for the full matrix. This sandbox's own container has no reachable Postgres/Docker daemon at all (a stricter instance of the pre-existing `PG17_COMPOSE_VALIDATION` environmental class) -- every DB-backed test in the ENTIRE repository (Phases 1-5 alike) skips cleanly, never fails; P5-10's own DB-backed CLI test is written and skips identically, substituted by a deterministic SYNTHETIC demonstration (`orchestration/phase_5/evidence/synthetic_copyability_demo.json`) run directly against the same production M1-M6 functions. 839 passed, 335 skipped, 0 failed across the full repository suite; ruff/mypy/format clean; single alembic head `0022`; 12/12 real-chain fixtures ok. No live/signing/credential/paid-provider path anywhere in this round's diff. See `orchestration/checkpoints/phase_5.md` for the complete builder self-audit. `last_orchestrator_approved_phase` is set to `4` by this same instruction's own `APPROVES_PHASE: 4` (Phase 4 approval), NOT self-approved by this row -- Phase 5 itself remains NOT orchestrator-approved pending independent audit of this submission. |

## Operational tooling

- `scripts/argus_orchestrator_watch.py` (`make orchestrator-watch`) — local
  "no-nudge" watcher: polls `orchestration/ORCHESTRATOR_INSTRUCTIONS.md` for
  a new `ACTIVE` instruction and, when one appears and passes the
  TARGET_COMMIT and phase-authorization checks, launches the local Claude
  CLI non-interactively to execute exactly that instruction under
  `orchestration/PROTOCOL.md`. Not running by default — the human operator
  starts it explicitly. See `docs/OPERATIONS.md` for usage.
  Evidence history:
  - `orchestration/checkpoints/watcher_setup.md` — original build/test record.
  - `orchestration/checkpoints/watcher_remediation.md` — a four-defect
    remediation pass (AUTHORIZED_PHASE was never validated; handoff
    instruction-id matching used substring containment instead of exact
    equality; checkpoint/bundle evidence was only checked for existence;
    a crash between CLAIMED and RUNNING was never recovered on restart).
  - `orchestration/checkpoints/watcher_remediation_2.md` — a second,
    orchestrator-requested remediation pass (instruction
    `argus-watcher-remediation-002`), rejecting the first pass as
    insufficient and requiring: strict fail-closed state handling
    (including a handoff-cross-check on state loss); a failed Claude
    process now always fails the run before any evidence is considered;
    evidence must be newly added, immutable, and structurally valid (not a
    placeholder); branch-movement/merge-commit/commit-attribution
    (`ARGUS-INSTRUCTION-ID:` trailer) verification; a blob-hash check that
    mechanically prevents the implementation agent from modifying
    `orchestration/ORCHESTRATOR_INSTRUCTIONS.md` during a run; explicit
    string-sequence phase gating (`0, 1, 1.5, 2, ..., 6, 6.5, ..., 11`) via
    a new `APPROVES_PHASE` field; strict instruction-field parsing; and a
    conservative, ordered post-run verification sequence. 26 new/updated
    adversarial regression tests.
  - `orchestration/checkpoints/watcher_remediation_3.md` — a third,
    orchestrator-requested remediation pass (instruction
    `argus-watcher-remediation-003`), rejecting round 2 as still fail-open
    in several places and requiring: a terminal, non-retryable
    `QUARANTINED` state (with a manual `--reset-quarantine` recovery
    procedure) the moment the instructions file is found modified —
    checked first and unconditionally, closing a bypass where a
    self-authored next instruction could otherwise launch on a later tick;
    tightened `TARGET_COMMIT` provenance (exactly one instruction-only
    commit whose parent is exactly the target; no `TARGET_COMMIT == HEAD`;
    no multi-commit or merge gaps); every safety-critical Git read now
    fails closed (`None`/explicit-failure) rather than defaulting to
    empty/clean/absent on a command error; commit attribution via a real
    `git interpret-trailers`-parsed terminal trailer, not text anywhere in
    body prose; broadened launch-exception handling that never leaves a
    stale `RUNNING` state, with raw Claude subprocess output never logged
    at all (only whitelisted metadata); real canonical-UTC timestamp
    parsing (not shape-only regex) for both instructions and handoffs; and
    tightened evidence linkage (exact checkpoint-bytes embedding in the
    bundle, exactly-once/full-SHA checkpoint identity fields, handoff
    `CURRENT_PHASE` matching `AUTHORIZED_PHASE`, required section
    headings). 74 total watcher tests (up from 51). This is operational
    tooling, not ARGUS phase work — `current_phase` above is unaffected.
  - Phase 1 remediation round 6 (instruction
    `argus-phase-1-remediation-006`, finding #5) added a final, fresh
    pre-launch remote-freshness barrier: `tick()`'s old
    `head_before`/`git_remote_head()` comparison read only the locally
    cached `origin/{branch}` ref set by the tick's early fetch, stale by
    however long instruction/target-commit/phase validation took. A new
    barrier performs a fresh fetch immediately before transitioning to
    `RUNNING`/launching Claude and re-verifies fetch success, worktree
    cleanliness, HEAD==freshly-fetched-remote-HEAD, an explicit
    working-tree-hash-vs-committed-blob snapshot of
    `ORCHESTRATOR_INSTRUCTIONS.md`, unchanged instruction fields,
    target-commit provenance, and phase authorization; any failure
    reverts to `IDLE` (never `FAILED`) without consuming the instruction,
    so the next tick freely re-evaluates. 77 total watcher tests (up
    from 74), including 3 new deterministic tests confirmed via `git
    stash` to genuinely launch Claude against the pre-fix watcher. This
    is operational tooling, not ARGUS phase work — `current_phase` above
    is unaffected.

## Rules

- This file may be updated by the implementation agent to reflect actual build
  progress.
- `last_orchestrator_approved_phase` and `approved_commit` may ONLY be set to a
  new value after receiving **explicit** orchestrator approval for that phase.
  The implementation agent must never self-approve a phase here.
- Do not begin work on `current_phase + 1` while `awaiting_orchestrator_review`
  is `true` for the current phase.
